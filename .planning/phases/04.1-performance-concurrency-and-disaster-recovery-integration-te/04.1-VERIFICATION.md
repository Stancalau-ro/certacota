---
phase: 04.1-performance-concurrency-and-disaster-recovery-integration-te
verified: 2026-05-15T00:00:00Z
status: human_needed
score: 18/21 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run ./gradlew :engine-service:disasterRecoveryTest after applying CR-01, CR-02, CR-03 fixes from REVIEW.md"
    expected: "Both postgresTransientOutageRecovery and redisRestartReconciliation tests pass; token conservation asserts true; JSON report written to target/perf-reports/disaster-recovery-*.json"
    why_human: "DisasterRecoveryIT has runtime assertion bugs (CR-01: probe stream leaks tokens, CR-03: no finally guard on Redis restart) that cannot be confirmed fixed without executing the test against live Testcontainers"
  - test: "Run ./gradlew :engine-service:performanceTest --tests '*.LargeDbStressIT' after applying CR-02 fix from REVIEW.md"
    expected: "queryLatencyUnderLargeDataset passes; teardown token conservation asserts true (delta=0 after subtracting totalDebited)"
    why_human: "LargeDbStressIT conservation formula at line 215 ignores totalDebited; every successful debit makes delta > 0 causing the assertion to fail. Fix is one-line (subtract totalDebited.get()) but correctness must be confirmed by execution"
  - test: "Run ./gradlew :engine-service:performanceTest after all fixes to confirm no regression in concurrency suite"
    expected: "ConcurrentSessionsIT, ConcurrentEndByTagIT, RakeConcurrencyIT, MixedConcurrencyIT all pass; 4 JSON reports written"
    why_human: "Concurrency tests require live Testcontainers (Postgres + Redis) and parallel thread execution; correctness of token conservation and deadlock detection can only be confirmed at runtime"
gaps:
  - truth: "DisasterRecoveryIT token conservation assertion passes at runtime (conserved == true)"
    status: failed
    reason: "CR-01: probe stream dr-redis-503-probe is started on dr-redis-acc-1 in redisRestartReconciliation() at line 209 but never stopped. Its in-flight balance deduction is not counted in totalSettled, so delta = initialTokensTotal - totalFinalBalance - totalSettled is negative, making conserved=false and the assertion at line 302 always fail. CR-03: the Redis restart sequence has no try/finally; if the probe stream REST call throws, startContainerCmd is never called, leaving Redis permanently stopped."
    artifacts:
      - path: "engine-service/src/test/java/com/certacota/engine/service/DisasterRecoveryIT.java"
        issue: "Lines 202-211: probe stream started but never stopped; no try/finally around stopContainerCmd/startContainerCmd block"
    missing:
      - "Stop the dr-redis-503-probe stream after Redis restart and accumulate its settledAmount into totalSettled (CR-01 fix)"
      - "Wrap the Redis stop/probe/start sequence in try/finally to guarantee startContainerCmd is always called (CR-03 fix)"
  - truth: "LargeDbStressIT token conservation assertion passes at runtime (conserved == true)"
    status: failed
    reason: "CR-02: teardown() computes delta = initialTokensTotal.subtract(totalFinalBalance) at line 215 and asserts it equals zero. However, the test issues up to 50 debit operations in queryLatencyUnderLargeDataset(); each successful debit reduces account balances tracked in the engine's accounts table. totalDebited is tracked but never subtracted. For every successful debit, delta > 0 and the conservation assertion fails."
    artifacts:
      - path: "engine-service/src/test/java/com/certacota/engine/service/perf/LargeDbStressIT.java"
        issue: "Line 215: delta = initialTokensTotal.subtract(totalFinalBalance) — missing .subtract(totalDebited.get())"
    missing:
      - "Change line 215 to: BigDecimal delta = initialTokensTotal.subtract(totalFinalBalance).subtract(totalDebited.get())"
  - truth: "DisasterRecoveryIT JSON report correctnessVerdicts accurately reflects measured test outcomes"
    status: failed
    reason: "WR-01: correctnessVerdicts map in teardown() hardcodes 'noFailedSettlements', true and 'reconciliationVerified', true regardless of actual scenario outcome. The local boolean variables noFailedSettlements and reconciliationVerified are scoped to their respective test methods and cannot be referenced from teardown(). The report always emits true even if settlements failed."
    artifacts:
      - path: "engine-service/src/test/java/com/certacota/engine/service/DisasterRecoveryIT.java"
        issue: "Lines 292-298: correctnessVerdicts hardcodes noFailedSettlements=true and reconciliationVerified=true; these are not fields, they are local method variables"
    missing:
      - "Promote noFailedSettlements and reconciliationVerified to AtomicBoolean fields; reference them in teardown()"
---

# Phase 04.1: Performance, Concurrency, and DR Integration Test Suite — Verification Report

**Phase Goal:** Build a standalone integration test suite that exercises the engine under realistic load, high concurrency, large data volumes, and partial failure conditions. Tests are driven by externalised configuration so scenarios can be tuned without code changes. Coverage must include: high-concurrency token flows, large-database stress, and component-failure recovery (transient connection loss and full outage).

**Verified:** 2026-05-15T00:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Config-driven test properties bound via @ConfigurationProperties(prefix="certacota.perf") covering all five scenario groups | VERIFIED | PerfTestProperties.java line 9: @ConfigurationProperties(prefix = "certacota.perf"); all 5 nested classes (Concurrency, Mixed, Stress, EndByTag, Dr) present; application-perf-test.yml has matching kebab-case keys |
| 2 | Gradle test task excludes performance/concurrency/disaster-recovery tags from the default test run | VERIFIED | build.gradle lines 40-41: useJUnitPlatform { excludeTags 'disaster-recovery', 'performance', 'concurrency' }; existing Cucumber system properties preserved |
| 3 | performanceTest and disasterRecoveryTest Gradle tasks exist with correct tag filters | VERIFIED | build.gradle lines 49-63: performanceTest uses includeTags 'performance | concurrency'; disasterRecoveryTest uses includeTags 'disaster-recovery' |
| 4 | LatencyRecorder computes p50/p95/p99/max using ConcurrentLinkedQueue (thread-safe) | VERIFIED | LatencyRecorder.java: ConcurrentLinkedQueue<Long>, sorted array percentile computation, nested Snapshot record — correct implementation |
| 5 | PerfReportWriter writes valid JSON to target/perf-reports/{scenario}-{timestamp}.json | VERIFIED | PerfReportWriter.java: Paths.get("target/perf-reports"), Files.createDirectories, DateTimeFormatter yyyyMMdd-HHmmss, Jackson ObjectMapper with INDENT_OUTPUT |
| 6 | DrContainerHolder exposes static Postgres + Redis containers via getPostgresContainer()/getRedisContainer() with @DynamicPropertySource wiring | VERIFIED | DrContainerHolder.java: static final PostgreSQLContainer<?>, static final GenericContainer<?>, both started in static block, @DynamicPropertySource wires JDBC URL + Redis host/port; public accessors present |
| 7 | seed.sh performs bulk seeding via generate_series() into stress_accounts + stress_audit_log at container init time | VERIFIED | seed.sh: CREATE TABLE IF NOT EXISTS stress_accounts, generate_series(1, ${ACCOUNTS}), CREATE TABLE IF NOT EXISTS stress_audit_log, generate_series(1, ${ACCOUNTS} * ${TXN_PER_ACCOUNT}), LPAD format, initial balance 1000000.000000000000000000 |
| 8 | target/perf-reports/ is gitignored | VERIFIED | .gitignore line 41: target/perf-reports/ |
| 9 | ConcurrentSessionsIT runs N parallel streaming sessions with randomised durations and asserts no deadlock + token conservation | VERIFIED | ConcurrentSessionsIT.java: CountDownLatch + ExecutorService pattern, ThreadLocalRandom.current().nextLong(minMs, maxMs+1), BigDecimal compareTo(BigDecimal.ZERO)==0 conservation check |
| 10 | ConcurrentEndByTagIT runs concurrent end-by-tag calls against overlapping tag sets and asserts all matched streams settled without deadlock | VERIFIED | ConcurrentEndByTagIT.java: @TestMethodOrder + @Order(1) setup / @Order(2) concurrent calls; aggregate check via GET /api/v1/tags/{tag}/aggregate verifying inFlightDebit==0 |
| 11 | RakeConcurrencyIT runs N rake-enabled streams stopping simultaneously and asserts three-way debit/credit/credit balanced arithmetic | VERIFIED | RakeConcurrencyIT.java: rakeRate=0.1, toAccountId, platformAccountId; threeWayDelta = initialFrom.subtract(toFinalTotal).subtract(platformFinal).subtract(fromFinalTotal); assertThat(threeWayBalanced).isTrue() |
| 12 | All new IT classes call only public REST endpoints (no @Autowired internal Repository beans) | VERIFIED | grep of *IT.java for @Autowired.*Repository returns no matches; all access via RestTemplate to http://localhost:{port}/api/v1/... |
| 13 | All new IT classes write a JSON report to target/perf-reports/ at @AfterAll | VERIFIED | All 6 IT classes call PerfReportWriter.write(scenario, report) in @AfterAll; assertThat(reportFile).exists() in ConcurrentSessionsIT, DisasterRecoveryIT, LargeDbStressIT |
| 14 | MixedConcurrencyIT runs per-account organic event lifecycles with hot-account contention amplification | VERIFIED | MixedConcurrencyIT.java: CopyOnWriteArrayList accountIds, hotAccountIds, sampleInterval() divides by multiplier when isHot, per-account thread pool, deadline-based loop |
| 15 | LargeDbStressIT auto-builds seed Docker image (certacota-perf-db-a{N}-t{M}) and boots engine against that image via asCompatibleSubstituteFor | VERIFIED | LargeDbStressIT.java: IMAGE_NAME = "certacota-perf-db-a" + ACCOUNTS + "-t" + TXN_PER_ACCOUNT; ensureSeedImageExists using docker images -q + docker build; DockerImageName.parse(IMAGE_NAME).asCompatibleSubstituteFor("postgres"); @DynamicPropertySource overrides datasource |
| 16 | LargeDbStressIT asserts p95 read latency <= ceiling and write p95 <= 2x ceiling per D-07 | VERIFIED | LargeDbStressIT.java lines 188-192: readP95 <= ceiling; writeP95 <= ceiling * 2 |
| 17 | LargeDbStressIT asserts token conservation via JDBC SUM against stress_accounts (not N REST calls) | VERIFIED | LargeDbStressIT.java lines 200-213: SELECT COUNT(*) AS cnt, SUM(balance) AS sum_balance FROM stress_accounts WHERE id LIKE 'stress-acc-%'; JDBC direct via @Autowired DataSource |
| 18 | DisasterRecoveryIT covers Postgres pause/unpause scenario (D-02 transient outage) | VERIFIED | DisasterRecoveryIT.java lines 96-176: DockerClientFactory.lazyClient(), pauseContainerCmd, unpauseContainerCmd (in try/finally), settlement assertions after unpause |
| 19 | DisasterRecoveryIT covers Redis stop/start scenario (D-02 full restart with reconciliation) | VERIFIED (structure only) | DisasterRecoveryIT.java lines 178-259: stopContainerCmd + startContainerCmd via DockerClientFactory; stream settlement after restart; BUT CR-01 (probe stream leak) and CR-03 (no finally for restart) mean the test will fail at runtime |
| 20 | DisasterRecoveryIT token conservation assertion passes at runtime | FAILED | CR-01: probe stream dr-redis-503-probe started at line 209 but never stopped; its in-flight balance deduction never appears in totalSettled, making delta negative and conserved=false |
| 21 | LargeDbStressIT token conservation assertion passes at runtime | FAILED | CR-02: delta = initialTokensTotal.subtract(totalFinalBalance) at line 215 does not subtract totalDebited.get(); every successful debit makes delta > 0 causing the assertion to always fail |

**Score:** 18/21 truths verified (3 failed)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `engine-service/src/test/java/com/certacota/engine/service/perf/PerfTestProperties.java` | @ConfigurationProperties with 5 nested scenario classes | VERIFIED | All 5 nested classes present; Lombok @Getter @Setter; IntervalRange and AmountRange as reusable top-level static inner classes |
| `engine-service/src/test/resources/application-perf-test.yml` | YAML with certacota.perf.* tree | VERIFIED | All keys present with correct kebab-case; values match Java defaults |
| `engine-service/src/test/java/com/certacota/engine/service/perf/LatencyRecorder.java` | Thread-safe ConcurrentLinkedQueue-backed percentile recorder | VERIFIED | ConcurrentLinkedQueue<Long>, sorted-copy percentile computation, Snapshot record |
| `engine-service/src/test/java/com/certacota/engine/service/perf/PerfReportWriter.java` | Jackson JSON writer to target/perf-reports/ | VERIFIED | Static ObjectMapper with JavaTimeModule; Files.createDirectories; timestamp format yyyyMMdd-HHmmss |
| `engine-service/src/test/java/com/certacota/engine/service/DrContainerHolder.java` | Static Postgres+Redis containers with @DynamicPropertySource | VERIFIED | Both containers in static block; @DynamicPropertySource wires all 5 properties; public accessors |
| `engine-service/src/test/docker/perf-db/Dockerfile` | Postgres 17-alpine with ARG ACCOUNTS / ARG TXN_PER_ACCOUNT | VERIFIED | FROM postgres:17-alpine; ARG ACCOUNTS=1000; ARG TXN_PER_ACCOUNT=100; ENV propagation; COPY seed.sh; chmod +x |
| `engine-service/src/test/docker/perf-db/seed.sh` | Real generate_series() bulk-seed into stress_accounts + stress_audit_log | VERIFIED | Both CREATE TABLE + INSERT ... FROM generate_series(); LPAD format stress-acc-{6-digit}; initial balance 1000000.000000000000000000; index on audit log |
| `engine-service/build.gradle` | excludeTags on test + performanceTest + disasterRecoveryTest tasks | VERIFIED | All three task blocks present; existing cucumber/api.version/DOCKER_HOST properties preserved |
| `engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentSessionsIT.java` | N parallel sessions, random duration, token conservation | VERIFIED | @Tag("performance"), @EnableConfigurationProperties, ThreadLocalRandom, BigDecimal.compareTo, PerfReportWriter.write |
| `engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentEndByTagIT.java` | Concurrent end-by-tag, overlapping tags, aggregate check | VERIFIED | @Tag("performance") + @Tag("concurrency"), @TestMethodOrder, @Order(1)/@Order(2), EndByTagResponse, aggregate assertion |
| `engine-service/src/test/java/com/certacota/engine/service/perf/RakeConcurrencyIT.java` | N rake streams simultaneous stop, three-way balance | VERIFIED | @Tag("performance") + @Tag("concurrency"), rakeRate, toAccountId, platformAccountId, threeWayDelta, threeWayBalanced assertion |
| `engine-service/src/test/java/com/certacota/engine/service/perf/MixedConcurrencyIT.java` | Per-account organic simulation with hot-account amplification | VERIFIED | hotAccountIds, sampleInterval with multiplier, addAtomic CAS accumulation, conservation: initial + credited = final + settled + debited |
| `engine-service/src/test/java/com/certacota/engine/service/DisasterRecoveryIT.java` | Postgres pause + Redis restart DR scenarios | STUB (runtime) | Structural presence verified; CR-01 and CR-03 make runtime conservation assertion fail; WR-01 makes report hardcode verdicts as true |
| `engine-service/src/test/java/com/certacota/engine/service/perf/LargeDbStressIT.java` | Seeded-image boot, latency assertions, JDBC conservation | STUB (runtime) | Structural presence verified; CR-02 makes runtime conservation assertion fail for any successful debit |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| PerfTestProperties.java | application-perf-test.yml | @ConfigurationProperties(prefix="certacota.perf") Spring binding | WIRED | Prefix matches YAML top-level key; all nested properties have correct kebab-case equivalents |
| DrContainerHolder.java | Spring datasource + Redis properties | @DynamicPropertySource | WIRED | registerProperties() wires all 5 properties; postgres.getJdbcUrl(), getUsername(), getPassword(), redis.getHost(), redis.getMappedPort(6379) |
| build.gradle test task | JUnit Platform tag filtering | useJUnitPlatform { excludeTags } | WIRED | excludeTags 'disaster-recovery', 'performance', 'concurrency' confirmed |
| perf-db Dockerfile ARG ACCOUNTS / ARG TXN_PER_ACCOUNT | seed.sh generate_series() | ENV propagation, shell substitution via <<-EOSQL | WIRED | ARG + ENV lines present; unquoted heredoc terminator EOSQL allows shell to expand ${ACCOUNTS} and ${TXN_PER_ACCOUNT} |
| All 6 IT classes | PerfReportWriter.write() | @AfterAll report serialization | WIRED | PerfReportWriter.write(scenario, reportMap) called in every teardown() |
| All 6 IT classes | GET /api/v1/accounts/{id} | RestTemplate balance fetch for token conservation | WIRED | Present in all IT teardown methods |
| ConcurrentSessionsIT | PerfTestProperties.Concurrency | @EnableConfigurationProperties + @Autowired | WIRED | config.getConcurrency().getSessions().getCount(), .getMinDurationMs(), .getMaxDurationMs() all used |
| DisasterRecoveryIT | DrContainerHolder.getPostgresContainer() + getRedisContainer() | Static accessor + DockerClientFactory.lazyClient() | WIRED | pauseContainerCmd(DrContainerHolder.getPostgresContainer().getContainerId()), stopContainerCmd(DrContainerHolder.getRedisContainer().getContainerId()) |
| LargeDbStressIT seededPostgres | Spring datasource | @DynamicPropertySource (property source precedence over TestcontainersConfiguration) | WIRED | registerSeededPostgres() registers spring.datasource.url from seededPostgres::getJdbcUrl |
| LargeDbStressIT | ProcessBuilder docker build | ensureSeedImageExists method | WIRED | "docker", "images", "-q", imageName check; "docker", "build", "--build-arg", "ACCOUNTS=...", "--build-arg", "TXN_PER_ACCOUNT=..." on cache miss |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| ConcurrentSessionsIT | totalSettled (AtomicReference) | StopStreamResponse.settledAmount() from REST POST /streams/{id}/stop | Yes — real HTTP call to live engine | FLOWING |
| DisasterRecoveryIT | totalSettled | StopStreamResponse.settledAmount() — but probe stream dr-redis-503-probe never stopped | Partial — 3 stream settlements counted, probe stream leak uncounted | HOLLOW — probe stream leaks tokens from conservation pool |
| LargeDbStressIT | delta (conservation) | initialTokensTotal.subtract(totalFinalBalance) — totalDebited never subtracted | Disconnected — totalDebited field tracked but ignored in formula | HOLLOW_PROP — CR-02 causes delta != 0 for any successful debit |
| MixedConcurrencyIT | totalCredited, totalDebited, totalSettled | REST credit/debit/stop calls via addAtomic() CAS | Yes — CAS-based concurrent accumulation | FLOWING |
| LargeDbStressIT | totalFinalBalance | JDBC SELECT SUM(balance) FROM stress_accounts | Yes — real JDBC query against seeded container | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — all test artifacts require live Testcontainers (Postgres + Redis + Docker daemon) which cannot be started in this verification context. Behavioral verification is delegated to human verification items.

### Probe Execution

Step 7c: No probe scripts defined for this phase. SKIPPED.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | Plan 04 | DB connection loss simulated via Testcontainers pauseContainerCmd | SATISFIED | DisasterRecoveryIT: DockerClientFactory.lazyClient().pauseContainerCmd(pgContainerId).exec() |
| D-02 | Plan 04 | Two DR scenarios: transient Postgres outage + full Redis restart | SATISFIED (structure) | Both test methods present; runtime correctness blocked by CR-01, CR-03 |
| D-03 | Plan 04 | Postgres pause: streams keep running in-memory; settlement proceeds after unpause | SATISFIED (structure) | Test asserts all stops return 2xx after unpause; CR-WR-03 timing window noted but structural intent correct |
| D-04 | Plans 01, 04, 05 | DR tests tagged @Tag("disaster-recovery"), excluded from default test | SATISFIED | DisasterRecoveryIT: @Tag("disaster-recovery"); build.gradle excludeTags includes 'disaster-recovery' |
| D-05 | Plan 01 | @ConfigurationProperties bound to application-perf-test.yml, activated via @ActiveProfiles("perf-test") | SATISFIED | PerfTestProperties.java, application-perf-test.yml present; all IT classes use @ActiveProfiles({"test", "perf-test"}) + @EnableConfigurationProperties |
| D-06 | Plan 02 | Concurrency parameters including [minDurationMs, maxDurationMs] with per-session ThreadLocalRandom | SATISFIED | ConcurrentSessionsIT: ThreadLocalRandom.current().nextLong(minMs, maxMs + 1) |
| D-07 | Plans 01, 05 | Stress parameters: accounts, transactionsPerAccount, maxQueryLatencyMs | SATISFIED | PerfTestProperties.Stress fields; LargeDbStressIT lines 188-192: readP95 <= ceiling, writeP95 <= ceiling * 2 |
| D-08 | Plan 01 | Single application-perf-test.yml; no environment-specific profiles | SATISFIED | Single YAML file at src/test/resources/application-perf-test.yml |
| D-09 | Plan 02 | Existing DiscreteTransactionConcurrencyTest and StreamingConcurrencyTest unchanged | SATISFIED | No modifications to those files; only new IT classes added |
| D-10 | Plans 02, 03 | Four new IT classes: ConcurrentSessionsIT, ConcurrentEndByTagIT, MixedConcurrencyIT, RakeConcurrencyIT | SATISFIED | All four files present with correct package and tags |
| D-11 | Plan 03 | Mixed scenario: per-account organic event lifecycles with independent threads | SATISFIED | MixedConcurrencyIT: Executors.newFixedThreadPool(totalAccounts), per-account runnable with event lifecycle |
| D-12 | Plan 03 | Hot accounts: interval divided by contentionMultiplier | SATISFIED | MixedConcurrencyIT.sampleInterval(): return (long) Math.max(1, base / multiplier) when isHot |
| D-13 | All plans | REST-only: no @Autowired internal Repository beans in any IT class | SATISFIED | grep @Autowired.*Repository across *IT.java: no matches. LargeDbStressIT @Autowired DataSource is documented D-13 exception for bulk SUM |
| D-14 | Plans 02, 03, 04, 05 | Latency capture: p50/p95/p99/max via LatencyRecorder | SATISFIED | LatencyRecorder present in all IT classes; snapshot() called in @AfterAll |
| D-15 | Plans 01, 05 | Engine boots against locally-built seeded Postgres image | SATISFIED | LargeDbStressIT: DockerImageName.parse(IMAGE_NAME).asCompatibleSubstituteFor("postgres"); new PostgreSQLContainer<>(seededImage) |
| D-16 | Plans 01, 05 | Cache-or-build: docker images -q check before docker build | SATISFIED | ensureSeedImageExists: ProcessBuilder("docker", "images", "-q", imageName) check; build only on cache miss |
| D-17 | Plan 01 | seed.sh performs real generate_series() bulk insert at container init time | SATISFIED | seed.sh: INSERT INTO stress_accounts FROM generate_series(1, ${ACCOUNTS}); INSERT INTO stress_audit_log FROM generate_series(1, ${ACCOUNTS} * ${TXN_PER_ACCOUNT}) |
| D-18 | All plans | JSON report to target/perf-reports/{scenario}-{yyyyMMdd-HHmmss}.json at @AfterAll | SATISFIED | PerfReportWriter.write() called in all 6 IT @AfterAll methods |
| D-19 | All plans | Report schema: scenario, timestamp, scenarioConfig, latency, operationCounts, correctnessVerdicts, tokenConservation | SATISFIED | All required fields present in every IT's report map |
| D-20 | All plans | Token conservation required assertion: conserved=false fails the test | SATISFIED (structure) | All IT classes call assertThat(conserved).isTrue(); formula is correct in ConcurrentSessionsIT, ConcurrentEndByTagIT, RakeConcurrencyIT, MixedConcurrencyIT; FAILED in DisasterRecoveryIT (CR-01) and LargeDbStressIT (CR-02) at runtime |
| D-21 | Plan 01 | Naming convention allows jq diff comparison between runs | SATISFIED | {scenario}-{yyyyMMdd-HHmmss}.json format implemented by PerfReportWriter |
| SC-1 | Plan 02 | High-concurrency suite tests: all matched streams settle, no deadlock | SATISFIED (structure) | ConcurrentEndByTagIT asserts inFlightDebit==0 for each tag after concurrent end-by-tag; deadlock canary via done.await(60, SECONDS) |
| SC-2 | Plans 02, 03 | High-concurrency suite: N parallel sessions without data corruption or deadlocks | SATISFIED (structure) | ConcurrentSessionsIT + MixedConcurrencyIT + RakeConcurrencyIT all use CountDownLatch deadlock canary; BigDecimal.compareTo conservation |
| SC-3 | Plan 05 | Large-database suite: query/write performance against seeded dataset | SATISFIED (structure) | LargeDbStressIT: 100 read probes + 50 write probes; p95 latency assertions; runtime blocked by CR-02 |
| SC-4 | Plan 04 | Recovery suite: engine resumes correct state after transient outage and restart | SATISFIED (structure) | DisasterRecoveryIT: both scenarios present; runtime correctness blocked by CR-01, CR-03 |
| SC-5 | All plans | Structured JSON reports produced for each scenario | SATISFIED | All 6 IT classes write reports; PerfReportWriter verified substantive |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| DisasterRecoveryIT.java | 202-211 | Probe stream started but never stopped; no try/finally on Redis restart | BLOCKER | CR-01: token conservation assertion always fails; CR-03: Redis can be left permanently stopped on test failure |
| LargeDbStressIT.java | 215 | `delta = initialTokensTotal.subtract(totalFinalBalance)` — totalDebited never subtracted | BLOCKER | CR-02: conservation assertion fails whenever any debit operation succeeds |
| DisasterRecoveryIT.java | 292-298 | `"noFailedSettlements", true` and `"reconciliationVerified", true` hardcoded in report map | WARNING | WR-01: report always claims success regardless of actual test outcome |
| MixedConcurrencyIT.java | 243-244 | `resp.getBody().settledAmount()` called without null check on settledAmount() itself | WARNING | WR-02: NPE if settledAmount field is null in response; NPE increments errorCount, silently corrupts totalSettled |
| DisasterRecoveryIT.java | 149-164 | expectedPerStream = rate * totalElapsedMs — does not subtract pause duration | WARNING | WR-03: with 3s pause out of ~4s window, expected total is ~75% higher than actual settled; ±10% tolerance structurally fails |
| LargeDbStressIT.java | 103-107 | checkProcess.waitFor() return value discarded | WARNING | WR-04: docker unavailable produces empty imageId, falls through to build which also fails with misleading error |
| ConcurrentSessionsIT.java, ConcurrentEndByTagIT.java, RakeConcurrencyIT.java | ~155-158 | executor.shutdown() called after assertThat(completed).isTrue() — not in finally | WARNING | WR-05: executor threads leak if done.await() times out and assertion fails |
| seed.sh | 37 | `$((${ACCOUNTS} * ${TXN_PER_ACCOUNT}))` — shell arithmetic in echo statement | INFO | CR-04: on 32-bit sh (busybox Alpine variant) this can overflow for large values; does not affect actual generate_series Postgres SQL; log message only |

### Human Verification Required

#### 1. DisasterRecoveryIT runtime correctness after CR-01, CR-02, CR-03 fixes

**Test:** Apply the three code fixes from REVIEW.md (CR-01: stop probe stream before settlement loop; CR-02: subtract totalDebited from delta; CR-03: wrap Redis stop/start in try/finally), then run `./gradlew :engine-service:disasterRecoveryTest`

**Expected:** Both test methods pass; JSON report written with `tokenConservation.conserved: true` and `correctnessVerdicts` reflecting actual measured outcomes (not hardcoded true)

**Why human:** Requires live Docker daemon + Testcontainers Postgres and Redis; the container pause/stop/start lifecycle cannot be simulated statically

#### 2. LargeDbStressIT runtime correctness after CR-02 fix

**Test:** Apply the fix `BigDecimal delta = initialTokensTotal.subtract(totalFinalBalance).subtract(totalDebited.get())` at LargeDbStressIT line 215, then run `./gradlew :engine-service:performanceTest --tests '*.LargeDbStressIT'`

**Expected:** Test passes; p95 latency within ceiling; `tokenConservation.conserved: true` in report; image build occurs on first run, cache hit on second run

**Why human:** Requires live Docker daemon to build the seeded image and run the PostgreSQLContainer; latency assertions depend on actual machine performance

#### 3. Full concurrency suite green run

**Test:** After all bug fixes applied, run `./gradlew :engine-service:performanceTest`

**Expected:** ConcurrentSessionsIT, ConcurrentEndByTagIT, RakeConcurrencyIT, MixedConcurrencyIT all pass; 4 JSON reports in engine-service/target/perf-reports/; all report `tokenConservation.conserved: true`

**Why human:** Concurrent test correctness (no deadlock, token conservation under parallel threads) requires execution against live Testcontainers-backed engine; deterministic reproduction requires running the full suite

### Gaps Summary

Three runtime-assertion bugs — all introduced by the executor and documented in REVIEW.md as CR-01, CR-02, and CR-03 — block the phase goal from being observably achieved. The test infrastructure is structurally complete: all 11 artifact files exist at the specified paths, all contain substantive implementations (not stubs), and all key links between configuration, test classes, Gradle tasks, Docker images, and report writers are wired correctly. The concurrency suite (Plans 02 and 03: 4 IT classes) has no known runtime bugs and is expected to pass.

The two DR/stress bugs (CR-01 in DisasterRecoveryIT, CR-02 in LargeDbStressIT) will cause token conservation assertions to fail on every run, making SC-3 and SC-4 goals unverifiable at runtime until fixed. After the three REVIEW.md fixes are applied, human test execution is required to confirm the full suite passes end-to-end.

**Root cause grouping:** CR-01 and CR-03 share the same root location (DisasterRecoveryIT.redisRestartReconciliation); CR-02 is a one-line formula fix in LargeDbStressIT.teardown(). All three are isolated to the two DR-phase test classes and do not affect the concurrency suite or test infrastructure utilities.

---

_Verified: 2026-05-15T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
