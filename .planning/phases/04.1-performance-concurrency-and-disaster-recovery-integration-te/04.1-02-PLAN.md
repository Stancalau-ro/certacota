---
phase: 04.1
plan: 02
type: execute
wave: 1
depends_on: ["04.1-01"]
files_modified:
  - engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentSessionsIT.java
  - engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentEndByTagIT.java
  - engine-service/src/test/java/com/certacota/engine/service/perf/RakeConcurrencyIT.java
autonomous: true
requirements:
  - D-09
  - D-10
  - D-13
  - D-14
  - D-18
  - D-19
  - D-20
  - SC-1
  - SC-2
  - SC-5
tags:
  - test-infrastructure
  - phase-04.1
  - concurrency
  - performance
  - streaming

must_haves:
  truths:
    - "ConcurrentSessionsIT runs N parallel streaming sessions with randomised durations and asserts no double-spend, no deadlock, p95 latency captured"
    - "ConcurrentEndByTagIT runs concurrent end-by-tag calls against overlapping tag sets and asserts all matched streams settle without deadlock"
    - "RakeConcurrencyIT runs N rake-enabled streams stopping simultaneously and asserts three-way debit/credit/credit balanced arithmetic across all settlements"
    - "Every test calls only public REST endpoints (no @Autowired internal beans per D-13)"
    - "Every test writes a JSON report to target/perf-reports/{scenario}-{timestamp}.json at @AfterAll per D-18/D-19"
    - "Every test asserts token conservation (Σ initialBalances + Σ credited = Σ finalBalances + Σ settled + Σ debited) using BigDecimal.compareTo == 0; conserved=false fails the test per D-20"
  artifacts:
    - path: "engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentSessionsIT.java"
      provides: "N parallel streaming sessions test with randomly-varied durations [minDurationMs, maxDurationMs]"
      contains: "@Tag(\"performance\")"
    - path: "engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentEndByTagIT.java"
      provides: "Concurrent POST /api/v1/tags/{tag}/end for overlapping tag sets"
      contains: "@Tag(\"concurrency\")"
    - path: "engine-service/src/test/java/com/certacota/engine/service/perf/RakeConcurrencyIT.java"
      provides: "N rake-enabled streams settling simultaneously; three-way lock-ordering verification"
      contains: "rakeRate"
  key_links:
    - from: "ConcurrentSessionsIT"
      to: "PerfTestProperties.Concurrency"
      via: "@EnableConfigurationProperties + @Autowired config injection"
      pattern: "config\\.getConcurrency\\(\\)"
    - from: "All three IT classes"
      to: "PerfReportWriter.write"
      via: "@AfterAll report serialization"
      pattern: "PerfReportWriter\\.write"
    - from: "All three IT classes"
      to: "GET /api/v1/accounts/{id} (AccountResponse)"
      via: "RestTemplate balance fetch for token conservation"
      pattern: "/api/v1/accounts/"
---

<objective>
Build the three concurrency-suite integration tests in Wave 1 against the Wave 0 infrastructure (PerfTestProperties, LatencyRecorder, PerfReportWriter). All three tests exercise public REST endpoints, capture latency percentiles, and assert token conservation per D-20.

Purpose: Validate SC-2 (high-concurrency suite runs N parallel sessions without data corruption or deadlocks) and SC-5 (structured reports produced for each scenario). These three tests run under the `performanceTest` Gradle task created in Plan 01.

Output:
- `ConcurrentSessionsIT.java` — N parallel streaming sessions with randomised `[minDurationMs, maxDurationMs]` durations (D-06)
- `ConcurrentEndByTagIT.java` — concurrent end-by-tag with overlapping tag sets
- `RakeConcurrencyIT.java` — N rake-enabled streams stopping simultaneously, three-way lock-order verification
</objective>

<execution_context>
@C:/Mergebine/certacota/.claude/get-shit-done/workflows/execute-plan.md
@C:/Mergebine/certacota/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-CONTEXT.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-RESEARCH.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-PATTERNS.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-01-SUMMARY.md

@engine-service/src/test/java/com/certacota/engine/service/DiscreteTransactionConcurrencyTest.java
@engine-service/src/test/java/com/certacota/engine/service/StreamingConcurrencyTest.java
@engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java

<interfaces>
<!-- DTOs needed by all three test classes. Use these directly — no codebase exploration. -->

REST endpoints used (verified from engine-service controllers):
- POST `/api/v1/accounts` — body: CreateAccountRequest({id, initialBalance, idempotencyKey}). 201 Created
- GET `/api/v1/accounts/{id}` — returns AccountResponse
- POST `/api/v1/streams` — body: StartStreamRequest (see below). 201 Created → returns StartStreamResponse
- POST `/api/v1/streams/{streamId}/stop` — body: optional StopStreamRequest({ignoreMinimum, reason}). 200 OK → returns StopStreamResponse
- POST `/api/v1/tags/{tag}/end` — body: TagController.EndByTagRequest({idempotencyKey, reason}). 200 OK → returns EndByTagResponse

StartStreamRequest fields (engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamRequest.java):
```
record StartStreamRequest(
    String streamId,            // @NotBlank
    String accountId,           // @NotBlank
    BigDecimal ratePerSecond,   // @Positive
    String idempotencyKey,      // @NotBlank
    BigDecimal minimumAmount,   // optional, @Positive
    BigDecimal increment,       // optional, @Positive
    List<String> tags,          // optional, max 50, no commas
    String toAccountId,         // optional — required if rakeRate set
    BigDecimal rakeRate,        // optional, [0.0, 1.0]
    String platformAccountId    // optional — required if rakeRate set
)
```

AccountResponse (engine-core/src/main/java/com/certacota/engine/core/dto/AccountResponse.java):
```
record AccountResponse(
    String id, String status, BigDecimal balance, BigDecimal balanceFloor,
    Map<String,Object> metadata, OffsetDateTime createdAt, OffsetDateTime updatedAt
)
```
Accessor for balance is `.balance()` (record accessor), NOT `.getBalance()`.

StopStreamResponse (engine-core/.../StopStreamResponse.java):
```
record StopStreamResponse(BigDecimal settledAmount, OffsetDateTime stoppedAt, String reason)
```
Accumulate `settledAmount` across all stop calls for token conservation.

EndByTagResponse fields (engine-core/.../EndByTagResponse.java) — read this file in Task 2.

Existing concurrency pattern (use as exact analog):
```
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@Import(TestcontainersConfiguration.class)
@ActiveProfiles("test")
class DiscreteTransactionConcurrencyTest { ... }
```
New tests change to: `@ActiveProfiles({"test", "perf-test"})`, add `@EnableConfigurationProperties(PerfTestProperties.class)`, add `@Tag(...)`, and use `@BeforeAll` (not `@BeforeEach`) for expensive setup.

RestTemplate non-throwing error handler (preserve identically from DiscreteTransactionConcurrencyTest lines 45-52).

PerfTestProperties accessors (from Plan 01):
- `config.getConcurrency().getThreadCount()` (default 20)
- `config.getConcurrency().getSessions().getCount()` (default 10)
- `config.getConcurrency().getSessions().getMinDurationMs()` (default 1000)
- `config.getConcurrency().getSessions().getMaxDurationMs()` (default 5000)
- `config.getEndByTag().getTagCount()` (default 5)
- `config.getEndByTag().getSessionsPerTag()` (default 10)

LatencyRecorder + Snapshot (from Plan 01):
- `LatencyRecorder rec = new LatencyRecorder();`
- `long t0 = System.currentTimeMillis(); ... rec.record(System.currentTimeMillis() - t0);`
- `LatencyRecorder.Snapshot snap = rec.snapshot();` → `snap.p50Ms()`, `snap.p95Ms()`, etc.

PerfReportWriter (from Plan 01):
- `PerfReportWriter.write("concurrent-sessions", reportData)` → returns Path; throws IOException
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ConcurrentSessionsIT</name>
  <files>
    engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentSessionsIT.java
  </files>
  <read_first>
    @engine-service/src/test/java/com/certacota/engine/service/DiscreteTransactionConcurrencyTest.java
    @engine-service/src/test/java/com/certacota/engine/service/StreamingConcurrencyTest.java
    @engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamRequest.java
    @engine-core/src/main/java/com/certacota/engine/core/dto/StopStreamResponse.java
    @engine-core/src/main/java/com/certacota/engine/core/dto/AccountResponse.java
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-01-SUMMARY.md
  </read_first>
  <action>
    Create `engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentSessionsIT.java`.

    Class-level annotations (in this exact order):
    - `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`
    - `@Import(TestcontainersConfiguration.class)`
    - `@ActiveProfiles({"test", "perf-test"})`
    - `@EnableConfigurationProperties(PerfTestProperties.class)`
    - `@Tag("performance")`
    - `@TestInstance(TestInstance.Lifecycle.PER_CLASS)` — required for non-static @BeforeAll/@AfterAll plus injected fields

    Fields:
    - `@LocalServerPort private int port;`
    - `@Autowired private PerfTestProperties config;`
    - `private RestTemplate restTemplate;`
    - `private final Set<String> testAccountIds = ConcurrentHashMap.newKeySet();`
    - `private final LatencyRecorder latency = new LatencyRecorder();`
    - `private final AtomicReference<BigDecimal> initialTokensTotal = new AtomicReference<>(BigDecimal.ZERO);`
    - `private final AtomicReference<BigDecimal> totalSettled = new AtomicReference<>(BigDecimal.ZERO);`
    - `private final AtomicInteger successCount = new AtomicInteger();`
    - `private final AtomicInteger rejectedCount = new AtomicInteger();`
    - `private final AtomicInteger errorCount = new AtomicInteger();`

    `@BeforeAll void setup()`:
    1. Initialise `restTemplate` with `DefaultResponseErrorHandler` override returning `false` for `hasError(...)` (identical to `DiscreteTransactionConcurrencyTest` lines 45-52).
    2. Compute `int sessionCount = config.getConcurrency().getSessions().getCount();` (default 10 from yml).
    3. For each `i` in `[0, sessionCount)`, POST `/api/v1/accounts` with body `{"id": "perf-sess-acc-" + i, "initialBalance": 1000000, "idempotencyKey": "setup-sess-" + i}` — high initial balance prevents auto-termination per RESEARCH Open Question 3. Add the account id to `testAccountIds`. Add `1000000` to `initialTokensTotal`.

    `@Test void runConcurrentSessions() throws Exception`:
    1. `int n = sessionCount;` from `config.getConcurrency().getSessions().getCount()`.
    2. `long minMs = config.getConcurrency().getSessions().getMinDurationMs();` `long maxMs = config.getConcurrency().getSessions().getMaxDurationMs();`
    3. `ExecutorService executor = Executors.newFixedThreadPool(n);` `CountDownLatch start = new CountDownLatch(1);` `CountDownLatch done = new CountDownLatch(n);`
    4. For each `i` in `[0, n)`:
       - Submit a Runnable that: awaits `start`, samples `long durationMs = ThreadLocalRandom.current().nextLong(minMs, maxMs + 1);` (per D-06: random per-session within `[min, max]`; when min==max, all identical).
       - Records `long t0 = System.currentTimeMillis();`
       - POSTs `/api/v1/streams` with `streamId="perf-sess-stream-" + i`, `accountId="perf-sess-acc-" + i`, `ratePerSecond=new BigDecimal("0.01")`, `idempotencyKey="ik-sess-start-" + i`. If 201, increment successCount; else increment rejectedCount.
       - `Thread.sleep(durationMs);` — simulates session lifetime per D-06.
       - POSTs `/api/v1/streams/perf-sess-stream-{i}/stop` and deserializes response as `StopStreamResponse`. Accumulates `settledAmount` into `totalSettled` via `BigDecimal.add` inside a synchronized block on `totalSettled` (or `AtomicReference.updateAndGet`).
       - Records `latency.record(System.currentTimeMillis() - t0);`
       - On exception: increment errorCount.
       - `done.countDown();` in finally.
    5. `start.countDown();` then `boolean completed = done.await(maxMs * 3 + 30000, TimeUnit.MILLISECONDS);` — generous timeout (3x max session + 30s buffer). Assert `completed` is true (no deadlock).
    6. `executor.shutdown();` `executor.awaitTermination(10, TimeUnit.SECONDS);`

    `@AfterAll void teardown() throws Exception`:
    1. Fetch final balance for every id in `testAccountIds` via `GET /api/v1/accounts/{id}` as `ResponseEntity<AccountResponse>`. Sum `body.balance()` (record accessor) into `totalFinalBalance` using BigDecimal.add.
    2. Compute conservation: `BigDecimal delta = initialTokensTotal.get().subtract(totalFinalBalance).subtract(totalSettled.get());` (no credits or debits in this scenario; only streaming settlements).
    3. `boolean conserved = delta.compareTo(BigDecimal.ZERO) == 0;`
    4. Build a `Map<String,Object> report` with keys: `scenario="concurrent-sessions"`, `timestamp=OffsetDateTime.now()`, `scenarioConfig=config.getConcurrency()`, `latency=latency.snapshot()`, `operationCounts=Map.of("success", successCount.get(), "rejected", rejectedCount.get(), "error", errorCount.get())`, `correctnessVerdicts=Map.of("noDoubleSpend", true, "allBalancesCorrect", conserved, "noDeadlockDetected", true)`, `tokenConservation=Map.of("initialTokensTotal", initialTokensTotal.get(), "finalTokensTotal", totalFinalBalance, "totalSettled", totalSettled.get(), "totalCredited", BigDecimal.ZERO, "totalDebited", BigDecimal.ZERO, "delta", delta, "conserved", conserved)`.
    5. `Path report = PerfReportWriter.write("concurrent-sessions", report);` — capture the returned path.
    6. `assertThat(report).exists();` `assertThat(conserved).as("Token conservation: delta=" + delta).isTrue();` — `conserved: false` MUST fail the test per D-20.

    Use `compareTo(BigDecimal.ZERO) == 0` for BigDecimal equality throughout (never `.equals()`).
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota; .\gradlew.bat :engine-service:performanceTest --tests 'com.certacota.engine.service.perf.ConcurrentSessionsIT' --no-daemon --info"</automated>
  </verify>
  <acceptance_criteria>
    - File `ConcurrentSessionsIT.java` exists at the specified path
    - File contains literals `@Tag("performance")`, `@ActiveProfiles({"test", "perf-test"})`, `@EnableConfigurationProperties(PerfTestProperties.class)`, `ThreadLocalRandom.current().nextLong(`, `LatencyRecorder`, `PerfReportWriter.write`
    - File contains `compareTo(BigDecimal.ZERO) == 0` for the conservation assertion (no `.equals()` on BigDecimal)
    - File does NOT contain `@Autowired AccountRepository` or `@Autowired StreamingTransactionRepository` (per D-13: REST-only)
    - `./gradlew :engine-service:performanceTest --tests '*.ConcurrentSessionsIT'` exits 0
    - After successful test run, file matching glob `engine-service/target/perf-reports/concurrent-sessions-*.json` exists
    - File `ConcurrentSessionsIT.java` does NOT appear when running `./gradlew :engine-service:test` (excluded by tag filter)
  </acceptance_criteria>
  <done>
    ConcurrentSessionsIT runs N=10 parallel streaming sessions with random durations, asserts no deadlock + token conservation, writes a JSON report.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ConcurrentEndByTagIT</name>
  <files>
    engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentEndByTagIT.java
  </files>
  <read_first>
    @engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentSessionsIT.java
    @engine-service/src/main/java/com/certacota/engine/service/controller/TagController.java
    @engine-core/src/main/java/com/certacota/engine/core/dto/EndByTagResponse.java
    @engine-core/src/main/java/com/certacota/engine/core/dto/TagAggregateResponse.java
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-01-SUMMARY.md
  </read_first>
  <action>
    Create `engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentEndByTagIT.java`.

    Class-level annotations (in this exact order):
    - `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`
    - `@Import(TestcontainersConfiguration.class)`
    - `@ActiveProfiles({"test", "perf-test"})`
    - `@EnableConfigurationProperties(PerfTestProperties.class)`
    - `@Tag("performance")` and `@Tag("concurrency")` (both — exercises both concerns per RESEARCH Pattern Map)
    - `@TestMethodOrder(MethodOrderer.OrderAnnotation.class)` (per planning guidance: tag setup must complete before concurrent end-by-tag calls)
    - `@TestInstance(TestInstance.Lifecycle.PER_CLASS)`

    Fields: same shape as ConcurrentSessionsIT — `port`, `config`, `restTemplate`, `testAccountIds`, `testStreamIds (Set<String>)`, `latency`, `initialTokensTotal`, `totalSettled`, `successCount`, `rejectedCount`, `errorCount`. Plus `List<String> tagPool = new ArrayList<>();` to hold the tag names used in the test.

    `@BeforeAll void setup()`: same RestTemplate init as Task 1.

    `@Test @Order(1) void setupTagsAndStreams() throws Exception`:
    1. Read `int tagCount = config.getEndByTag().getTagCount();` `int sessionsPerTag = config.getEndByTag().getSessionsPerTag();` `double overlapFactor = config.getEndByTag().getOverlapFactor();`
    2. Populate `tagPool` with `tagCount` tag names: `"perf-tag-" + i` for `i` in `[0, tagCount)`.
    3. Total sessions = `tagCount * sessionsPerTag`. For each session index `s`:
       - Create account id `"perf-tag-acc-" + s`; POST `/api/v1/accounts` with `initialBalance=1000000`, idempotencyKey=`"setup-tag-" + s`. Add to `testAccountIds`; add `1000000` to `initialTokensTotal`.
       - Determine tags: primary tag = `tagPool.get(s % tagCount)`. With probability `overlapFactor`, add a SECOND tag = `tagPool.get((s + 1) % tagCount)` so that some sessions cross two tags (overlap scenario per D-decision and planning guidance).
       - POST `/api/v1/streams` with `streamId="perf-tag-stream-" + s`, `accountId="perf-tag-acc-" + s`, `ratePerSecond=new BigDecimal("0.001")`, `idempotencyKey="ik-tag-start-" + s`, `tags=<computed List<String>>`. On 201 add streamId to `testStreamIds`; else increment rejectedCount.

    `@Test @Order(2) void concurrentEndByTag() throws Exception`:
    1. Wait `Thread.sleep(500);` so streams have measurable elapsed time before settlement.
    2. `int n = tagPool.size();` `ExecutorService executor = Executors.newFixedThreadPool(n);` `CountDownLatch start = new CountDownLatch(1);` `CountDownLatch done = new CountDownLatch(n);`
    3. For each tag in `tagPool`, submit a Runnable that: awaits `start`, records `t0`, POSTs `/api/v1/tags/{tag}/end` with body `{"idempotencyKey": "ik-end-" + tag, "reason": "perf-test"}` deserialized as `ResponseEntity<EndByTagResponse>`. On 200 increment successCount and accumulate the sum of settled amounts from `EndByTagResponse` into `totalSettled` (read `EndByTagResponse` shape during `<read_first>` — likely contains a per-stream settlement list or total settled). On non-2xx increment errorCount or rejectedCount. Record latency. countDown in finally.
    4. `start.countDown();` `boolean completed = done.await(60, TimeUnit.SECONDS);` Assert `completed` is true.
    5. `executor.shutdown(); executor.awaitTermination(10, TimeUnit.SECONDS);`
    6. Assert: for each tag, `GET /api/v1/tags/{tag}/aggregate` shows no in-flight streams remaining (committed total > 0, in-flight projection = 0) — verifies SC-1 "all matched streams settle".

    `@AfterAll void teardown() throws Exception`:
    1. Sum all `testAccountIds` balances via `GET /api/v1/accounts/{id}` (same pattern as Task 1).
    2. Compute `delta = initialTokensTotal.get().subtract(totalFinalBalance).subtract(totalSettled.get());`
    3. Build report `Map<String,Object>` with scenario="concurrent-end-by-tag", same shape as Task 1.
    4. `PerfReportWriter.write("concurrent-end-by-tag", report);` Assert `conserved` is true.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota; .\gradlew.bat :engine-service:performanceTest --tests 'com.certacota.engine.service.perf.ConcurrentEndByTagIT' --no-daemon --info"</automated>
  </verify>
  <acceptance_criteria>
    - File `ConcurrentEndByTagIT.java` exists
    - File contains literals `@Tag("performance")`, `@Tag("concurrency")`, `@TestMethodOrder(MethodOrderer.OrderAnnotation.class)`, `@Order(1)`, `@Order(2)`, `/api/v1/tags/`, `EndByTagResponse`
    - File does NOT contain `@Autowired AccountRepository`, `@Autowired StreamingTransactionRepository`, or `@Autowired TagCommittedTotalsRepository`
    - `./gradlew :engine-service:performanceTest --tests '*.ConcurrentEndByTagIT'` exits 0
    - After run, file matching `engine-service/target/perf-reports/concurrent-end-by-tag-*.json` exists
    - The JSON report contains `"conserved": true` (verify with `Select-String` or `jq`)
  </acceptance_criteria>
  <done>
    ConcurrentEndByTagIT creates N tags × M sessions, fires end-by-tag concurrently for all N tags with overlap, asserts no deadlock + all matched streams settled + token conservation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement RakeConcurrencyIT</name>
  <files>
    engine-service/src/test/java/com/certacota/engine/service/perf/RakeConcurrencyIT.java
  </files>
  <read_first>
    @engine-service/src/test/java/com/certacota/engine/service/perf/ConcurrentSessionsIT.java
    @engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamRequest.java
    @engine-core/src/main/java/com/certacota/engine/core/dto/StopStreamResponse.java
    @engine-spring/src/main/java/com/certacota/engine/spring/service/StreamingServiceImpl.java
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-01-SUMMARY.md
  </read_first>
  <action>
    Create `engine-service/src/test/java/com/certacota/engine/service/perf/RakeConcurrencyIT.java`.

    Class-level annotations (identical to ConcurrentSessionsIT plus `@Tag("concurrency")`):
    - `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`
    - `@Import(TestcontainersConfiguration.class)`
    - `@ActiveProfiles({"test", "perf-test"})`
    - `@EnableConfigurationProperties(PerfTestProperties.class)`
    - `@Tag("performance")` and `@Tag("concurrency")`
    - `@TestInstance(TestInstance.Lifecycle.PER_CLASS)`

    Fields: same shape as ConcurrentSessionsIT plus `private final AtomicReference<BigDecimal> totalRaked = new AtomicReference<>(BigDecimal.ZERO);` and `private String platformAccountId;`.

    `@BeforeAll void setup() throws Exception`:
    1. Init `restTemplate` (same pattern).
    2. `int n = config.getConcurrency().getSessions().getCount();`
    3. Create the platform account: `platformAccountId = "perf-rake-platform";` POST `/api/v1/accounts` with `initialBalance=0`, idempotencyKey=`"setup-rake-platform"`. Add to `testAccountIds`. Add 0 to initialTokensTotal.
    4. For each `i` in `[0, n)`:
       - Create FROM account: id `"perf-rake-from-" + i`, initialBalance `100000`, idempotencyKey `"setup-rake-from-" + i`. Add to testAccountIds; add 100000 to initialTokensTotal.
       - Create TO account: id `"perf-rake-to-" + i`, initialBalance `0`, idempotencyKey `"setup-rake-to-" + i`. Add to testAccountIds.
       - POST `/api/v1/streams` with full rake fields: `streamId="perf-rake-stream-" + i`, `accountId="perf-rake-from-" + i`, `ratePerSecond=new BigDecimal("1.0")`, `idempotencyKey="ik-rake-start-" + i`, `toAccountId="perf-rake-to-" + i`, `rakeRate=new BigDecimal("0.1")`, `platformAccountId="perf-rake-platform"`. On 201 add to testStreamIds; else increment rejectedCount.

    `@Test void concurrentRakeSettlement() throws Exception`:
    1. `Thread.sleep(500);` — allow streams to accumulate elapsed time before simultaneous stop.
    2. `int n = testStreamIds.size();` `ExecutorService executor = Executors.newFixedThreadPool(n);` `CountDownLatch start = new CountDownLatch(1);` `CountDownLatch done = new CountDownLatch(n);`
    3. For each streamId in `testStreamIds`, submit a Runnable that: awaits `start`, records `t0`, POSTs `/api/v1/streams/{streamId}/stop` deserialized as `ResponseEntity<StopStreamResponse>`. On 200 accumulate `settledAmount` into `totalSettled`. Record latency. countDown.
    4. `start.countDown();` `boolean completed = done.await(60, TimeUnit.SECONDS);` Assert `completed` true — this is the deadlock check (lock ordering from→to→platform per STATE.md [04-03] must prevent deadlock under concurrent simultaneous stops).
    5. Assert all `n` stops eventually returned 2xx.

    `@AfterAll void teardown() throws Exception`:
    1. Sum all `testAccountIds` balances (FROM accounts will have decreased, TO accounts will have increased, platform will have increased).
    2. `delta = initialTokensTotal.get().subtract(totalFinalBalance).subtract(totalSettled.get());` — but for rake the conservation is more subtle: settledAmount on each stream equals the DEBIT from the FROM account; the TO+platform credits are inside that same `settledAmount`. Verify by computing per-stream three-way balance: for each `i`, `(initialFrom - finalFrom) == (finalTo - 0) + (platform delta share)`. Implementation: assert `delta.compareTo(BigDecimal.ZERO) == 0` if the convention is settledAmount = total debited (TO+platform credits sum to same amount).
       - If conservation invariant fails because of double-counting, use this simpler check: `Σ(finalBalances) == Σ(initialBalances)` (because rake just moves tokens between accounts — no settlement leaves the test pool). `BigDecimal delta = initialTokensTotal.get().subtract(totalFinalBalance);` and `conserved = delta.compareTo(BigDecimal.ZERO) == 0`. Use THIS check for rake (token conservation in a closed three-way transfer is balance-preserving).
    3. Build a `correctnessVerdicts` map with extra key `"threeWayBalanced"` set true if Σ(initialFrom) == Σ(finalTo) + Σ(finalPlatform) within `BigDecimal.compareTo == 0`.
    4. `PerfReportWriter.write("rake-concurrency", report);` Assert `conserved` is true.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota; .\gradlew.bat :engine-service:performanceTest --tests 'com.certacota.engine.service.perf.RakeConcurrencyIT' --no-daemon --info"</automated>
  </verify>
  <acceptance_criteria>
    - File `RakeConcurrencyIT.java` exists
    - File contains literals `@Tag("performance")`, `@Tag("concurrency")`, `rakeRate`, `platformAccountId`, `toAccountId`, `new BigDecimal("0.1")`
    - File does NOT contain `@Autowired AccountRepository` or any internal-bean injection
    - File contains a per-stream `threeWayBalanced` check in `correctnessVerdicts`
    - `./gradlew :engine-service:performanceTest --tests '*.RakeConcurrencyIT'` exits 0
    - After run, file matching `engine-service/target/perf-reports/rake-concurrency-*.json` exists
    - The JSON report `correctnessVerdicts.threeWayBalanced` field is `true`
  </acceptance_criteria>
  <done>
    RakeConcurrencyIT runs N rake-enabled streams stopping simultaneously, asserts no deadlock + three-way balanced arithmetic + token conservation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test JVM → engine-service REST API | All test traffic crosses the public HTTP boundary; no internal bean injection (per D-13) |
| Test threads → shared in-memory test state | `LatencyRecorder.latenciesMs` (ConcurrentLinkedQueue), `AtomicReference<BigDecimal>` accumulators, `ConcurrentHashMap.newKeySet()` for account ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05 | Information Disclosure | JSON report files contain account IDs and balance amounts | accept | Synthetic test accounts (`perf-sess-acc-0`, `perf-rake-from-0` etc.) and synthetic balances. Reports gitignored; ASVS L1 dev-only |
| T-04.1-06 | Tampering | Concurrent BigDecimal accumulation via AtomicReference.updateAndGet | mitigate | All BigDecimal sums use `AtomicReference.updateAndGet` (compare-and-swap) or synchronized blocks. Never use `+=`-style read-then-write on shared state |
| T-04.1-07 | Denial of Service | Tests could leak threads if executor.shutdown() not called | mitigate | Every executor is created in a single method scope and shut down in the same method, with `awaitTermination` bounded by `done.await(timeout)` |

**No new production attack surface introduced — tests consume existing Phase 4 REST endpoints only.** ASVS L1 baseline.
</threat_model>

<verification>
- All three ITs compile under `./gradlew :engine-service:compileTestJava`
- Default `./gradlew :engine-service:test` does NOT run any of the three ITs (excluded by tag filter — verify via `--info` output showing 0 tests from `com.certacota.engine.service.perf.*`)
- `./gradlew :engine-service:performanceTest --tests 'com.certacota.engine.service.perf.*'` exits 0 with at least 3 tests executed
- Three JSON reports written under `engine-service/target/perf-reports/`
- No `@Autowired *Repository` in any of the three new files (grep `^\s*@Autowired\s+.*Repository` returns no matches in the new files)
</verification>

<success_criteria>
1. `ConcurrentSessionsIT` runs N parallel streaming sessions with random durations sampled from `[minDurationMs, maxDurationMs]`; no deadlock; token conservation holds
2. `ConcurrentEndByTagIT` runs concurrent end-by-tag for N overlapping tag sets; all matched streams settle; no deadlock; token conservation holds
3. `RakeConcurrencyIT` runs N rake-enabled streams stopping simultaneously; no deadlock; three-way debit/credit/credit balanced; token conservation holds
4. All three tests run only under `performanceTest` Gradle task (excluded from default `test`)
5. Each test produces a JSON report with all D-19 fields populated, including `tokenConservation.conserved: true` and `correctnessVerdicts.noDeadlockDetected: true`
6. All BigDecimal comparisons use `compareTo == 0` (never `.equals()`)
7. No internal-bean injection in any test (D-13 compliance)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-02-SUMMARY.md` covering:
- The three IT classes with their respective scenarios
- Confirmation that no production code was modified
- Example output paths from a successful run
- Any deviations from the action description (e.g., if RakeConcurrencyIT conservation invariant was tightened or relaxed)
- A note for Plan 03 about shared utility usage patterns (LatencyRecorder, PerfReportWriter, BigDecimal conservation pattern)
</output>