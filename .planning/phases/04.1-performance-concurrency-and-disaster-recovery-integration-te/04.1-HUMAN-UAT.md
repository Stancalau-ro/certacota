---
status: complete
phase: 04.1-performance-concurrency-and-disaster-recovery-integration-te
source: [04.1-VERIFICATION.md]
started: 2026-05-15T12:30:00Z
updated: 2026-05-16T00:00:00Z
---

## Tests

### 1. Disaster Recovery — Postgres pause/unpause scenario
expected: DisasterRecoveryIT postgresTransientOutageRecovery() passes — streams settle with elapsed-time amounts; token conservation holds; JSON report shows conserved=true
result: PASSED — all 3 pg streams settled with 2xx; ±10% tolerance assertion passed; JSON report written

### 2. Disaster Recovery — Redis pause/unpause scenario
expected: DisasterRecoveryIT redisRestartReconciliation() passes — streams settle correctly after Redis unpause; token conservation holds; JSON report shows conserved=true
result: PASSED — all 3 redis streams settled with 2xx after Redis unpause; ±20% tolerance assertion passed; JSON report written

### 3. Large-DB stress — latency assertions
expected: LargeDbStressIT.largeDbStress() passes — read p95 ≤ 500ms, write p95 ≤ 1000ms; token conservation via JDBC SUM holds; JSON report written
result: PASSED — latency assertions within ceiling; JDBC SUM conservation delta=0; JSON report written

## Commands

```powershell
cd C:\Mergebine\certacota
.\gradlew.bat :engine-service:disasterRecoveryTest --no-daemon
.\gradlew.bat :engine-service:performanceTest --no-daemon
```

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
