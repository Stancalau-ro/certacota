---
status: partial
phase: 04.1-performance-concurrency-and-disaster-recovery-integration-te
source: [04.1-VERIFICATION.md]
started: 2026-05-15T12:30:00Z
updated: 2026-05-15T12:30:00Z
---

## Current Test

[awaiting review fixes then human test execution]

## Pre-conditions

Apply REVIEW.md fixes before running these tests:
- CR-01: Stop probe stream in DisasterRecoveryIT.redisRestartReconciliation() and accumulate settledAmount
- CR-02: Add `.subtract(totalDebited.get())` to LargeDbStressIT conservation formula
- CR-03: Wrap DisasterRecoveryIT Redis startContainerCmd in try/finally
- WR-01: Fix DisasterRecoveryIT report to capture actual boolean outcomes

Run: `/gsd:code-review 04.1 --fix` to auto-apply

## Tests

### 1. Disaster Recovery — Postgres pause/unpause scenario
expected: DisasterRecoveryIT postgresOutageRecovery() passes — streams settle with elapsed-time amounts (not pre-pause amounts); token conservation holds; JSON report shows conserved=true
result: [pending]

### 2. Disaster Recovery — Redis restart + reconciliation scenario
expected: DisasterRecoveryIT redisRestartReconciliation() passes — engine reconciles StreamRegistry from DB on next write; streams settle correctly; token conservation holds; JSON report shows conserved=true
result: [pending]

### 3. Large-DB stress — latency assertions
expected: LargeDbStressIT.largeDbStress() passes — read p95 ≤ 500ms, write p95 ≤ 1000ms; token conservation via JDBC SUM holds; JSON report written
result: [pending]

## Commands

```powershell
# Apply review fixes first
# Then run:
cd C:\Mergebine\certacota
.\gradlew.bat :engine-service:disasterRecoveryTest --no-daemon
.\gradlew.bat :engine-service:performanceTest --tests "*.LargeDbStressIT" --no-daemon
.\gradlew.bat :engine-service:performanceTest --no-daemon
```

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
