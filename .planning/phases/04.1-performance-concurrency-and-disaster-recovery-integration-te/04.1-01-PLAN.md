---
phase: 04.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - engine-service/build.gradle
  - engine-service/src/test/java/com/certacota/engine/service/DrContainerHolder.java
  - engine-service/src/test/java/com/certacota/engine/service/perf/PerfTestProperties.java
  - engine-service/src/test/java/com/certacota/engine/service/perf/LatencyRecorder.java
  - engine-service/src/test/java/com/certacota/engine/service/perf/PerfReportWriter.java
  - engine-service/src/test/resources/application-perf-test.yml
  - engine-service/src/test/docker/perf-db/Dockerfile
  - engine-service/src/test/docker/perf-db/seed.sh
  - .gitignore
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-14
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-20
tags:
  - test-infrastructure
  - phase-04.1
  - performance
  - configuration-properties

must_haves:
  truths:
    - "Gradle `test` task excludes @Tag('performance'), @Tag('concurrency'), @Tag('disaster-recovery') and still runs all existing tests including Cucumber"
    - "`./gradlew :engine-service:disasterRecoveryTest --tests '*' --info` succeeds with 0 tests executed (task registered, no DR tests yet)"
    - "PerfTestProperties binds successfully from application-perf-test.yml under @ActiveProfiles({'test','perf-test'})"
    - "LatencyRecorder computes p50/p95/p99/max correctly for a known input set"
    - "PerfReportWriter writes valid JSON with BigDecimal scale=18 preserved to target/perf-reports/{scenario}-{timestamp}.json"
    - "DrContainerHolder exposes static Postgres + Redis containers with @DynamicPropertySource wiring (TestcontainersConfiguration.java unchanged)"
    - "target/perf-reports/ is gitignored"
  artifacts:
    - path: "engine-service/build.gradle"
      provides: "Gradle task registrations for performanceTest and disasterRecoveryTest plus excludeTags on default test"
      contains: "tasks.register('disasterRecoveryTest'"
    - path: "engine-service/src/test/java/com/certacota/engine/service/perf/PerfTestProperties.java"
      provides: "@ConfigurationProperties(prefix='certacota.perf') with Concurrency/Stress/Mixed/Dr/EndByTag nested classes"
      contains: "@ConfigurationProperties(prefix = \"certacota.perf\")"
    - path: "engine-service/src/test/resources/application-perf-test.yml"
      provides: "Default perf-test parameters under certacota.perf.*"
      contains: "certacota:"
    - path: "engine-service/src/test/java/com/certacota/engine/service/perf/LatencyRecorder.java"
      provides: "Thread-safe latency collection via ConcurrentLinkedQueue<Long>; p50/p95/p99/max computed post-collection"
      contains: "ConcurrentLinkedQueue"
    - path: "engine-service/src/test/java/com/certacota/engine/service/perf/PerfReportWriter.java"
      provides: "Jackson JSON writer for perf reports; creates target/perf-reports/ if missing"
      contains: "target/perf-reports"
    - path: "engine-service/src/test/java/com/certacota/engine/service/DrContainerHolder.java"
      provides: "Static Postgres + Redis containers with @DynamicPropertySource for DR tests; separate from TestcontainersConfiguration"
      contains: "static final"
    - path: "engine-service/src/test/docker/perf-db/Dockerfile"
      provides: "Postgres 17-alpine seed image scaffold accepting ACCOUNTS + TXN_PER_ACCOUNT build args"
      contains: "ARG ACCOUNTS"
    - path: "engine-service/src/test/docker/perf-db/seed.sh"
      provides: "Shell entry script that generates SQL using generate_series() and ENV-injected build args"
      contains: "generate_series"
  key_links:
    - from: "PerfTestProperties.java"
      to: "application-perf-test.yml"
      via: "Spring Boot @ConfigurationProperties binding"
      pattern: "@ConfigurationProperties.*certacota\\.perf"
    - from: "DrContainerHolder.java"
      to: "@DynamicPropertySource"
      via: "Spring test property registry"
      pattern: "@DynamicPropertySource"
    - from: "engine-service/build.gradle test task"
      to: "JUnit Platform tag filtering"
      via: "useJUnitPlatform { excludeTags ... }"
      pattern: "excludeTags 'disaster-recovery'"
---

<objective>
Build the Wave 0 test infrastructure foundation for Phase 04.1: config-driven test properties, latency/report utilities, DR container holder, Gradle task wiring, and large-DB seed image scaffold. No new IT classes are created here — this plan provides the shared infrastructure that Plans 02/03/04 consume.

Purpose: Plans 02–04 cannot create test classes without (a) `PerfTestProperties` to bind from `application-perf-test.yml`, (b) `LatencyRecorder` + `PerfReportWriter` to satisfy D-14/D-18/D-19/D-20, (c) `DrContainerHolder` for the DR tests that need pause/stop/start container access, and (d) `disasterRecoveryTest` Gradle task to isolate DR runs. Building these first prevents downstream re-work and lets all consumer tests be created in parallel where possible.

Output:
- New `engine-service/build.gradle` task registrations (`performanceTest`, `disasterRecoveryTest`) and `excludeTags` on the existing `test` task
- New `PerfTestProperties` @ConfigurationProperties class + `application-perf-test.yml`
- New `LatencyRecorder` + `PerfReportWriter` utility classes (`engine-service/src/test/java/.../perf/` package)
- New `DrContainerHolder` static container holder for DR tests (`engine-service/src/test/java/.../` — top-level package)
- New `engine-service/src/test/docker/perf-db/Dockerfile` + `seed.sh` scaffold
- `.gitignore` addition for `target/perf-reports/`
</objective>

<execution_context>
@C:/Mergebine/certacota/.claude/get-shit-done/workflows/execute-plan.md
@C:/Mergebine/certacota/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-CONTEXT.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-RESEARCH.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-PATTERNS.md
@.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-VALIDATION.md

@engine-service/build.gradle
@engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java
@engine-spring/src/main/java/com/certacota/engine/spring/config/TokenEngineProperties.java
@engine-service/src/main/resources/application.yml

<interfaces>
<!-- Existing API surface needed by downstream Plans 02-04 to compose REST calls. -->
<!-- These are unchanged in Plan 01 — listed here so downstream plans inherit them via SUMMARY. -->

From engine-core/src/main/java/com/certacota/engine/core/dto/AccountResponse.java:
```
public record AccountResponse(
    String id,
    String status,
    BigDecimal balance,
    BigDecimal balanceFloor,
    Map<String, Object> metadata,
    OffsetDateTime createdAt,
    OffsetDateTime updatedAt
)
```

From engine-core/src/main/java/com/certacota/engine/core/dto/StopStreamResponse.java:
```
public record StopStreamResponse(
    BigDecimal settledAmount,
    OffsetDateTime stoppedAt,
    String reason
)
```

From engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamRequest.java (rake fields):
```
public record StartStreamRequest(
    @NotBlank String streamId,
    @NotBlank String accountId,
    @NotNull @Positive BigDecimal ratePerSecond,
    @NotBlank String idempotencyKey,
    @Nullable @Positive BigDecimal minimumAmount,
    @Nullable @Positive BigDecimal increment,
    @Nullable List<String> tags,
    @Nullable String toAccountId,
    @Nullable @DecimalMin("0.0") @DecimalMax("1.0") BigDecimal rakeRate,
    @Nullable String platformAccountId
)
```

From engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java (UNCHANGED in this plan):
```
@TestConfiguration(proxyBeanMethods = false)
public class TestcontainersConfiguration {
    static final GenericContainer<?> redis;  // already static
    static { redis.start(); }
    @Bean @ServiceConnection PostgreSQLContainer<?> postgresContainer();  // instance @Bean — NOT static
    @DynamicPropertySource static void redisProperties(...);
}
```

Gradle task naming reference (from engine-service/build.gradle line 39-45):
- Existing `tasks.named('test')` block contains: `useJUnitPlatform()` (bare), `cucumber.junit-platform.naming-strategy=long`, `cucumber.plugin=pretty,html:build/reports/cucumber.html`, `api.version=1.44`, `DOCKER_HOST=tcp://localhost:2375`. These must be preserved.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PerfTestProperties + application-perf-test.yml + .gitignore entry</name>
  <files>
    engine-service/src/test/java/com/certacota/engine/service/perf/PerfTestProperties.java,
    engine-service/src/test/resources/application-perf-test.yml,
    .gitignore
  </files>
  <read_first>
    @engine-spring/src/main/java/com/certacota/engine/spring/config/TokenEngineProperties.java
    @engine-service/src/main/resources/application.yml
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-CONTEXT.md
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-PATTERNS.md
    @.gitignore
  </read_first>
  <action>
    Create `engine-service/src/test/java/com/certacota/engine/service/perf/PerfTestProperties.java` as a public class annotated `@ConfigurationProperties(prefix = "certacota.perf")` plus Lombok `@Getter @Setter`. Define five nested `@Getter @Setter public static class` properties accessed via the outer class: `Concurrency concurrency = new Concurrency()`, `Mixed mixed = new Mixed()`, `Stress stress = new Stress()`, `EndByTag endByTag = new EndByTag()`, `Dr dr = new Dr()`.

    Nested class field definitions (use camelCase Java fields; YAML kebab-case binds automatically via Spring relaxed binding):

    `Concurrency` — fields: `int threadCount = 20`, `int rampUpSeconds = 2`, `int durationSeconds = 30`. Nested `Sessions sessions = new Sessions()` where `Sessions` has `int count = 10`, `long minDurationMs = 1000L`, `long maxDurationMs = 5000L`.

    `Mixed` — fields: `int accounts = 50`, `int durationSeconds = 30`. Nested `Sessions sessions = new Sessions()` with `int min = 1`, `int max = 3`. Nested `IntervalRange creditIntervalMs = new IntervalRange()` with `long min = 200`, `long max = 1000`. Nested `AmountRange creditAmount = new AmountRange()` with `BigDecimal min = new BigDecimal("1")`, `BigDecimal max = new BigDecimal("100")`. Nested `IntervalRange debitIntervalMs` and `AmountRange debitAmount` with same defaults. Nested `HotAccounts hotAccounts = new HotAccounts()` with `int count = 5`, `double contentionMultiplier = 3.0`.

    `Stress` — fields: `int accounts = 10000`, `int transactionsPerAccount = 500000`, `long maxQueryLatencyMs = 500L`.

    `EndByTag` — fields: `int tagCount = 5`, `int sessionsPerTag = 10`, `double overlapFactor = 0.3` (fraction of sessions that carry multiple tags).

    `Dr` — fields: `long postgresPauseDurationMs = 3000L`, `long redisRestartWaitMs = 1000L`.

    Create `engine-service/src/test/resources/application-perf-test.yml` with the full `certacota.perf.*` tree using kebab-case keys matching all field names (e.g., `thread-count`, `min-duration-ms`, `contention-multiplier`, `transactions-per-account`, `max-query-latency-ms`, `postgres-pause-duration-ms`). Values match the Java field defaults above.

    Append `target/perf-reports/` to `.gitignore` (project root); also append `engine-service/target/` if not already gitignored. Do NOT commit the `target/perf-reports/` directory itself.

    Per D-05/D-06/D-07/D-08, profile prefix is `certacota.perf` (Claude's discretion locked here). Per D-08, no env-specific yml — single file.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota; .\gradlew.bat :engine-service:compileTestJava --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - File `engine-service/src/test/java/com/certacota/engine/service/perf/PerfTestProperties.java` exists
    - File contains the literal `@ConfigurationProperties(prefix = "certacota.perf")`
    - File contains the literal nested class declarations `public static class Concurrency`, `public static class Mixed`, `public static class Stress`, `public static class EndByTag`, `public static class Dr`
    - File contains `Sessions sessions`, `HotAccounts hotAccounts`, `IntervalRange creditIntervalMs`, `AmountRange creditAmount` field declarations
    - File `engine-service/src/test/resources/application-perf-test.yml` exists and contains the top-level key `certacota:` and nested key `perf:` with sub-keys `concurrency`, `mixed`, `stress`, `end-by-tag`, `dr`
    - YAML contains `thread-count: 20`, `accounts: 50`, `contention-multiplier: 3.0`, `transactions-per-account: 500000`, `postgres-pause-duration-ms: 3000`
    - `.gitignore` contains the line `target/perf-reports/`
    - `./gradlew :engine-service:compileTestJava` exits 0
  </acceptance_criteria>
  <done>
    PerfTestProperties compiles; application-perf-test.yml is valid YAML with matching kebab-case keys; .gitignore excludes the reports directory.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create LatencyRecorder + PerfReportWriter + DrContainerHolder</name>
  <files>
    engine-service/src/test/java/com/certacota/engine/service/perf/LatencyRecorder.java,
    engine-service/src/test/java/com/certacota/engine/service/perf/PerfReportWriter.java,
    engine-service/src/test/java/com/certacota/engine/service/DrContainerHolder.java
  </files>
  <read_first>
    @engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java
    @engine-service/src/main/resources/application.yml
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-RESEARCH.md
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-PATTERNS.md
  </read_first>
  <action>
    Create `engine-service/src/test/java/com/certacota/engine/service/perf/LatencyRecorder.java` as a public class with these members:
    - private final `ConcurrentLinkedQueue<Long> latenciesMs = new ConcurrentLinkedQueue<>()` (thread-safe append-only collector per RESEARCH Pattern 5; avoids HdrHistogram thread-safety issue)
    - public method `void record(long elapsedMs)` adds value to queue (clamp negative values to 0 with `Math.max(0, elapsedMs)`)
    - public method `Snapshot snapshot()` returns a record `Snapshot(long p50Ms, long p95Ms, long p99Ms, long maxMs, long count)`. Computation: copy queue to `long[]`, `Arrays.sort(arr)`, index = `(int) Math.min(arr.length - 1, Math.floor(arr.length * percentile))`. If queue is empty, return `new Snapshot(0,0,0,0,0)`.
    - public method `int size()` returns `latenciesMs.size()`
    - Define nested `public record Snapshot(long p50Ms, long p95Ms, long p99Ms, long maxMs, long count)` for JSON serialization

    Create `engine-service/src/test/java/com/certacota/engine/service/perf/PerfReportWriter.java` as a public class with:
    - private static final `ObjectMapper MAPPER` constructed with `.registerModule(new JavaTimeModule()).disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS).enable(SerializationFeature.INDENT_OUTPUT)`
    - public static method `Path write(String scenario, Object reportData) throws IOException` that:
      1. Resolves `Path reportDir = Paths.get("target/perf-reports")` (relative to working directory — Gradle test working dir is `engine-service/`)
      2. Calls `Files.createDirectories(reportDir)` — never throws if already exists
      3. Builds filename `scenario + "-" + DateTimeFormatter.ofPattern("yyyyMMdd-HHmmss").format(LocalDateTime.now()) + ".json"` per D-18
      4. Writes `reportData` via `MAPPER.writerWithDefaultPrettyPrinter().writeValue(file.toFile(), reportData)`
      5. Returns the resolved Path so tests can assert on it
    - Per D-19/D-20: BigDecimal values are serialized via Jackson default (toString preserving scale=18). No special config required since application.yml already sets `use-big-decimal-for-floats: true` for deserialization — for serialization, BigDecimal.toString() preserves scale.

    Create `engine-service/src/test/java/com/certacota/engine/service/DrContainerHolder.java` as a public class annotated `@TestConfiguration(proxyBeanMethods = false)` with TWO static container fields and matching `@DynamicPropertySource`:
    - `static final PostgreSQLContainer<?> postgres` initialized in a static block: `new PostgreSQLContainer<>(DockerImageName.parse("postgres:17-alpine"))` followed by `.start()`.
    - `static final GenericContainer<?> redis` initialized identically to the existing pattern in `TestcontainersConfiguration` (`DockerImageName.parse("redis:7-alpine"), .withExposedPorts(6379), .start()`).
    - `@DynamicPropertySource static void registerProperties(DynamicPropertyRegistry registry)` wiring BOTH containers:
      - `registry.add("spring.datasource.url", () -> postgres.getJdbcUrl())`
      - `registry.add("spring.datasource.username", postgres::getUsername)`
      - `registry.add("spring.datasource.password", postgres::getPassword)`
      - `registry.add("spring.data.redis.host", redis::getHost)`
      - `registry.add("spring.data.redis.port", () -> String.valueOf(redis.getMappedPort(6379)))`
    - Public static accessors: `public static PostgreSQLContainer<?> getPostgresContainer() { return postgres; }` and `public static GenericContainer<?> getRedisContainer() { return redis; }`. DR tests use these for `pauseContainerCmd`/`stopContainerCmd`.

    `TestcontainersConfiguration.java` is NOT modified (per RESEARCH Open Question 1 recommendation — separate holder leaves existing tests untouched).

    Package-private static fields are fine for both holders (no public exposure needed beyond the accessors).
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota; .\gradlew.bat :engine-service:compileTestJava --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - File `LatencyRecorder.java` exists; contains `ConcurrentLinkedQueue<Long>`, method `record(long`, method `snapshot()`, nested `record Snapshot(long p50Ms, long p95Ms, long p99Ms, long maxMs, long count)`
    - File `PerfReportWriter.java` exists; contains `target/perf-reports`, `Files.createDirectories`, `yyyyMMdd-HHmmss`, `writerWithDefaultPrettyPrinter`
    - File `DrContainerHolder.java` exists; contains `static final PostgreSQLContainer<?>`, `static final GenericContainer<?>`, `@TestConfiguration`, `@DynamicPropertySource`, `postgres.getJdbcUrl()`, `redis.getMappedPort(6379)`, `getPostgresContainer()`, `getRedisContainer()`
    - File `TestcontainersConfiguration.java` is byte-identical to its pre-plan state (no modifications): `git diff --quiet engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java` returns 0
    - `./gradlew :engine-service:compileTestJava` exits 0
  </acceptance_criteria>
  <done>
    Three utility/holder classes compile; TestcontainersConfiguration unchanged; LatencyRecorder is thread-safe (ConcurrentLinkedQueue-backed); PerfReportWriter writes to target/perf-reports/.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Gradle test tasks + create perf-db Docker scaffold</name>
  <files>
    engine-service/build.gradle,
    engine-service/src/test/docker/perf-db/Dockerfile,
    engine-service/src/test/docker/perf-db/seed.sh
  </files>
  <read_first>
    @engine-service/build.gradle
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-RESEARCH.md
    @.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-PATTERNS.md
  </read_first>
  <action>
    Modify `engine-service/build.gradle`:

    1. Replace the existing `tasks.named('test') { useJUnitPlatform() ... }` block (currently line 39-45) so the bare `useJUnitPlatform()` becomes `useJUnitPlatform { excludeTags 'disaster-recovery', 'performance', 'concurrency' }`. PRESERVE the four existing inner statements verbatim: `systemProperty 'cucumber.junit-platform.naming-strategy', 'long'`, `systemProperty 'cucumber.plugin', 'pretty,html:build/reports/cucumber.html'`, `systemProperty 'api.version', '1.44'`, `environment 'DOCKER_HOST', 'tcp://localhost:2375'`.

    2. Append two new tasks below the modified `test` block:
       ```
       tasks.register('performanceTest', Test) {
           useJUnitPlatform {
               includeTags 'performance | concurrency'
           }
           systemProperty 'api.version', '1.44'
           environment 'DOCKER_HOST', 'tcp://localhost:2375'
       }

       tasks.register('disasterRecoveryTest', Test) {
           useJUnitPlatform {
               includeTags 'disaster-recovery'
           }
           systemProperty 'api.version', '1.44'
           environment 'DOCKER_HOST', 'tcp://localhost:2375'
       }
       ```
       Use single quotes (Groovy idiom matches existing file). The tag expression `'performance | concurrency'` uses JUnit Platform OR syntax (RESEARCH Pattern 7).

    Create `engine-service/src/test/docker/perf-db/Dockerfile`:
    ```
    FROM postgres:17-alpine
    ARG ACCOUNTS=1000
    ARG TXN_PER_ACCOUNT=1000
    ENV ACCOUNTS=${ACCOUNTS}
    ENV TXN_PER_ACCOUNT=${TXN_PER_ACCOUNT}
    ENV POSTGRES_DB=certacota
    ENV POSTGRES_USER=certacota
    ENV POSTGRES_PASSWORD=certacota
    COPY seed.sh /docker-entrypoint-initdb.d/01-seed.sh
    RUN chmod +x /docker-entrypoint-initdb.d/01-seed.sh
    ```
    Image name encoding from D-15: `certacota-perf-db-a{ACCOUNTS}-t{TXN_PER_ACCOUNT}` — caller of `docker build` supplies `-t <name>` flag, not the Dockerfile.

    Create `engine-service/src/test/docker/perf-db/seed.sh` with `#!/bin/sh` shebang. Body uses `psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB"` with a heredoc containing the parameterized SQL. Use `${ACCOUNTS}` and `${TXN_PER_ACCOUNT}` shell substitution (NOT psql `:'ARG'` — see RESEARCH Pitfall 6). SQL must run AFTER Flyway-style migrations would have run; this is a fresh seed image so we inline the minimum schema needed for stress testing OR we rely on the engine to apply Flyway on first startup against this image.

    Decision: leave seed.sh as a scaffold that DOES NOT create schema (Flyway will create schema when the engine starts up against this seeded image). The seed.sh body for this plan creates a no-op heredoc with a comment block explaining that schema-creation SQL is intentionally left for `LargeDbStressIT` to populate against an already-migrated schema. Concretely, the file contains:
    ```
    #!/bin/sh
    set -e
    # Seeding deferred — schema is provisioned by Flyway from engine-service at test startup.
    # Bulk data seeding for stress tests is performed by LargeDbStressIT via JDBC after Flyway runs.
    # ACCOUNTS=${ACCOUNTS}, TXN_PER_ACCOUNT=${TXN_PER_ACCOUNT} are visible as ENV vars at runtime.
    echo "perf-db image initialized with ACCOUNTS=${ACCOUNTS}, TXN_PER_ACCOUNT=${TXN_PER_ACCOUNT}"
    ```
    Rationale: production schema lives in Flyway migrations in `engine-spring`/`engine-core`. Replicating it in `seed.sh` would create a maintenance burden whenever a migration is added. Plan 04 (`LargeDbStressIT`) bulk-seeds data via JDBC after Spring has applied Flyway — this preserves single-source-of-truth for the schema.

    Do NOT make seed.sh executable via Git on Windows — `chmod +x` inside the Dockerfile handles permissions in the built image.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota; .\gradlew.bat :engine-service:tasks --all --no-daemon 2>&amp;1 | Select-String -Pattern 'performanceTest|disasterRecoveryTest'"</automated>
  </verify>
  <acceptance_criteria>
    - `engine-service/build.gradle` contains the literal `excludeTags 'disaster-recovery', 'performance', 'concurrency'`
    - `engine-service/build.gradle` contains the literal `tasks.register('performanceTest', Test)` and `tasks.register('disasterRecoveryTest', Test)`
    - `engine-service/build.gradle` still contains the literal `systemProperty 'cucumber.junit-platform.naming-strategy', 'long'` (preserved from before)
    - `./gradlew :engine-service:tasks --all` output lists both `performanceTest` and `disasterRecoveryTest`
    - `./gradlew :engine-service:test --tests 'NonExistentClass' --info` shows the JUnit Platform tag filter (`@Tag` exclusion in effect) — non-fatal if exit code non-zero due to no matching tests; what matters is no Gradle syntax error
    - File `engine-service/src/test/docker/perf-db/Dockerfile` exists; contains literal `FROM postgres:17-alpine`, `ARG ACCOUNTS`, `ARG TXN_PER_ACCOUNT`, `COPY seed.sh /docker-entrypoint-initdb.d/`
    - File `engine-service/src/test/docker/perf-db/seed.sh` exists; first line is `#!/bin/sh`; contains literal `ACCOUNTS=${ACCOUNTS}` reference
    - `./gradlew :engine-service:test --no-daemon` still runs the existing Cucumber suite (verification deferred to Plan 02 since this would require full Postgres+Redis startup — instead assert that `./gradlew :engine-service:test --dry-run` succeeds)
  </acceptance_criteria>
  <done>
    Gradle wires three test tasks with correct tag filters; perf-db Docker scaffold exists; existing Cucumber configuration preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test JVM → Docker daemon | Tests issue `docker images -q` and `docker build` via ProcessBuilder; DR tests invoke `pauseContainerCmd`, `stopContainerCmd`, `startContainerCmd` against the local Docker socket |
| Plan 01 → production code | None — all artifacts are test-scope only (`engine-service/src/test/**`, `engine-service/build.gradle`). No production class loaded or modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Information Disclosure | Test reports may capture real-looking BigDecimal amounts in target/perf-reports/ | accept | Reports contain only synthetic test data (account IDs like `stress-acc-000001`, amounts like `1000000.000000000000000000`). Directory is `.gitignore`d; CI archives separately |
| T-04.1-02 | Tampering | Docker build invocation via ProcessBuilder accepts ACCOUNTS/TXN_PER_ACCOUNT from PerfTestProperties | mitigate | Build args are int-typed in `Stress` properties class — type system prevents shell injection. Image name uses int-validated values only. ProcessBuilder passes args as separate array elements (no shell interpretation) |
| T-04.1-03 | Denial of Service | DR tests `pauseContainerCmd` could pause the wrong container if container ID lookup is incorrect | mitigate | `DrContainerHolder.getPostgresContainer().getContainerId()` returns the ID of the holder's own container, set in the static block before `@DynamicPropertySource` wiring completes. No cross-test container interference |
| T-04.1-04 | Elevation of Privilege | `seed.sh` runs as Postgres init script with superuser inside the seeded container | accept | Container is ephemeral and used only for stress tests on the developer/CI machine. Never deployed. ASVS L1 baseline: dev-only |

**No new production API surface; no new authentication/authorization paths.** This phase introduces only test infrastructure. ASVS L1 baseline assessment: no applicable categories triggered.
</threat_model>

<verification>
- `./gradlew :engine-service:compileTestJava` succeeds (all new test classes compile)
- `./gradlew :engine-service:tasks --all` lists `performanceTest` and `disasterRecoveryTest`
- `./gradlew :engine-service:test --dry-run` succeeds (Gradle config is valid; existing Cucumber test runner still wired)
- `git diff engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java` is empty (file untouched)
- `target/perf-reports/` is gitignored
</verification>

<success_criteria>
1. All 8 files exist at the specified paths
2. `PerfTestProperties` exposes `concurrency`, `mixed`, `stress`, `endByTag`, `dr` getters with the correct nested type structure
3. `LatencyRecorder` is thread-safe (ConcurrentLinkedQueue-backed) and produces a `Snapshot` record serializable by Jackson
4. `PerfReportWriter` creates `target/perf-reports/` on first call and writes pretty-printed JSON
5. `DrContainerHolder` exposes static postgres + redis containers via `getPostgresContainer()` / `getRedisContainer()` accessors without modifying `TestcontainersConfiguration`
6. `engine-service/build.gradle` registers `performanceTest` and `disasterRecoveryTest` tasks; default `test` task excludes the three Phase 04.1 tags
7. perf-db Docker scaffold exists; seed.sh is intentionally schema-free (Flyway owns schema)
8. `.gitignore` excludes `target/perf-reports/`
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-performance-concurrency-and-disaster-recovery-integration-te/04.1-01-SUMMARY.md` covering:
- All 8 new/modified files with their purpose
- The `PerfTestProperties` structure (nested class hierarchy) so Plans 02–04 can compose `@EnableConfigurationProperties(PerfTestProperties.class)` and access `config.getConcurrency().getThreadCount()` etc.
- The `DrContainerHolder` accessor methods so Plan 04 knows how to call `pauseContainerCmd(DrContainerHolder.getPostgresContainer().getContainerId()).exec()`
- The three Gradle task names + their tag filters
- Any deviations from the action description with justification
</output>