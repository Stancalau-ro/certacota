---
phase: 03-streaming-transactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine-service/src/main/resources/db/migration/V7__create_streaming_transactions.sql
  - engine-service/src/main/resources/db/migration/V8__create_shedlock.sql
  - engine-service/src/main/resources/db/migration/V9__create_audit_archive.sql
  - engine-spring/build.gradle
  - engine-service/build.gradle
  - engine-core/src/main/java/com/certacota/engine/core/domain/StreamingTransaction.java
  - engine-core/src/main/java/com/certacota/engine/core/domain/StreamState.java
  - engine-core/src/main/java/com/certacota/engine/core/repository/StreamingTransactionRepository.java
  - engine-core/src/main/java/com/certacota/engine/core/service/StreamingService.java
  - engine-core/src/main/java/com/certacota/engine/core/service/StreamRegistry.java
  - engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamRequest.java
  - engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamResponse.java
  - engine-core/src/main/java/com/certacota/engine/core/dto/StopStreamRequest.java
  - engine-core/src/main/java/com/certacota/engine/core/dto/StopStreamResponse.java
  - engine-core/src/main/java/com/certacota/engine/core/dto/EstimatedBalanceResponse.java
  - engine-core/src/main/java/com/certacota/engine/core/exception/StreamNotFoundException.java
  - engine-core/src/main/java/com/certacota/engine/core/exception/RedisUnavailableException.java
  - engine-core/src/main/java/com/certacota/engine/core/exception/StreamAlreadyActiveException.java
  - engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java
  - engine-service/src/test/resources/features/streaming-start.feature
  - engine-service/src/test/resources/features/streaming-stop.feature
  - engine-service/src/test/resources/features/streaming-estimation.feature
  - engine-service/src/test/resources/features/streaming-minimum-amount.feature
  - engine-service/src/test/resources/features/streaming-increment.feature
  - engine-service/src/test/resources/features/streaming-auto-termination.feature
  - engine-service/src/test/java/com/certacota/engine/service/steps/StreamingSteps.java
autonomous: true
requirements:
  - STR-01
  - STR-02
  - STR-03
  - STR-04
  - STR-05
  - STR-06
  - STR-07
  - STR-08
  - STR-09
  - AUTO-01
  - AUTO-02
  - AUTO-03
  - BAL-02

must_haves:
  truths:
    - "Flyway V7 creates streaming_transactions with UNIQUE(stream_id), FK to accounts, CHECK (rate_per_second > 0), and status CHECK constraint"
    - "Flyway V8 creates shedlock table with exact 4-column schema required by ShedLock"
    - "Flyway V9 creates audit_archive schema and audit_archive.balance_audit_log mirror table with no FK back to public schema"
    - "engine-spring and engine-service build.gradle declare spring-boot-starter-data-redis, redisson-spring-boot-starter:3.50.0, shedlock-spring:6.6.0, shedlock-provider-jdbc-template:6.6.0"
    - "StreamingTransaction JPA entity maps streaming_transactions with all immutable columns updatable=false; status and settled fields are mutable"
    - "StreamState Java record carries startedAtEpochMillis, startedAtNano, startedAtNanoFromCurrentJvm, and static fromRedis() factory"
    - "StreamRegistry interface declares register, get, remove, getActiveStreams, hasActiveStreams with no Spring or Redis imports"
    - "StreamingService interface declares startStream, stopStream, estimateBalance, autoTerminate with correct return types"
    - "All 5 DTO records exist with exact field names from D-07 through D-12"
    - "All 3 exception classes exist extending RuntimeException with single-arg constructors"
    - "TestcontainersConfiguration adds static GenericContainer redis field and @DynamicPropertySource registering spring.data.redis.host and spring.data.redis.port"
    - "All 6 Cucumber feature files exist with at least one Scenario per file covering the target requirement"
    - "StreamingSteps.java contains @Given/@When/@Then annotations matching the step signatures in all 6 feature files; all methods throw PendingException"
  artifacts:
    - path: "engine-service/src/main/resources/db/migration/V7__create_streaming_transactions.sql"
      provides: "streaming_transactions DDL"
      contains: "UNIQUE (stream_id)"
    - path: "engine-service/src/main/resources/db/migration/V8__create_shedlock.sql"
      provides: "shedlock DDL"
      contains: "lock_until"
    - path: "engine-service/src/main/resources/db/migration/V9__create_audit_archive.sql"
      provides: "audit_archive schema + mirror table"
      contains: "audit_archive"
    - path: "engine-core/src/main/java/com/certacota/engine/core/domain/StreamingTransaction.java"
      provides: "JPA entity"
      exports: ["StreamingTransaction"]
    - path: "engine-core/src/main/java/com/certacota/engine/core/domain/StreamState.java"
      provides: "in-memory value object"
      exports: ["StreamState"]
    - path: "engine-core/src/main/java/com/certacota/engine/core/service/StreamRegistry.java"
      provides: "Redis registry port interface"
      exports: ["StreamRegistry"]
    - path: "engine-core/src/main/java/com/certacota/engine/core/service/StreamingService.java"
      provides: "streaming service interface"
      exports: ["startStream", "stopStream", "estimateBalance", "autoTerminate"]
    - path: "engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java"
      provides: "Redis Testcontainers container"
      contains: "redisContainer"
  key_links:
    - from: "StreamingTransaction.status"
      to: "streaming_transactions.status"
      via: "@Column(name = 'status') with @Enumerated(EnumType.STRING)"
      pattern: "status"
    - from: "TestcontainersConfiguration.redisContainer"
      to: "spring.data.redis.host/port"
      via: "@DynamicPropertySource"
      pattern: "DynamicPropertySource"
    - from: "StreamRegistry.register"
      to: "RedisStreamRegistry (Plan 02)"
      via: "port interface — implementation in engine-spring"
      pattern: "StreamRegistry"
---

<objective>
Establish the complete Wave 1 foundation for Phase 3: Flyway DDL migrations create the streaming_transactions, shedlock, and audit_archive tables; build files gain the Redis + Redisson + ShedLock dependencies; engine-core gains all contracts (entity, value object, repository, service interfaces, DTOs, exceptions); and the test scaffolding (Redis Testcontainers, all 6 feature files, StreamingSteps with pending step stubs) is in place so subsequent plans build against real types and tests run immediately (RED).

Purpose: Contracts-first ordering identical to Phase 2 Plan 01. Plan 02 executor sees exact type signatures and never guesses method names or field shapes. Feature files go RED immediately so the Nyquist verification loop has feedback from Plan 02 onward.

Output: 3 SQL migrations, 4 build file changes, 10 engine-core Java files (new), 8 test files (new/modified).
</objective>

<execution_context>
@C:/Mergebine/certacota/.claude/get-shit-done/workflows/execute-plan.md
@C:/Mergebine/certacota/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-streaming-transactions/03-CONTEXT.md
@.planning/phases/03-streaming-transactions/03-RESEARCH.md
@.planning/phases/03-streaming-transactions/03-PATTERNS.md

Prior plan context:
@.planning/phases/02-discrete-transactions/02-01-SUMMARY.md

<interfaces>
From engine-core/src/main/java/com/certacota/engine/core/domain/DiscreteTransaction.java (exact analog for StreamingTransaction):
- JPA annotations: @Entity, @Table, @Getter, @Builder, @NoArgsConstructor, @AllArgsConstructor
- @Id @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id
- @Column(precision = 38, scale = 18) for all BigDecimal fields
- @Column(updatable = false) on all immutable fields
- @Enumerated(EnumType.STRING) for enum columns
- All timestamps use OffsetDateTime

From engine-core/src/main/java/com/certacota/engine/core/dto/CreditRequest.java (exact analog for StartStreamRequest):
- Java record with jakarta.validation.constraints.* on fields
- @NotNull @Positive BigDecimal amount — apply same to ratePerSecond
- Optional fields (minimumAmount, increment) have @Positive but no @NotNull

From engine-core/src/main/java/com/certacota/engine/core/dto/PostTransactionResponse.java (analog for Start/StopStreamResponse):
- Java record with static from(Entity txn) factory method

From engine-core/src/main/java/com/certacota/engine/core/exception/AccountNotFoundException.java:
- public class AccountNotFoundException extends RuntimeException
- Single constructor: public AccountNotFoundException(String id) { super("Account not found: " + id); }

From engine-core/src/main/java/com/certacota/engine/core/repository/AccountRepository.java:
- extends JpaRepository with custom JPQL @Query methods
- findWithLock: @Lock(LockModeType.PESSIMISTIC_WRITE) @Query("SELECT a FROM Account a WHERE a.id = :id")

From engine-core/src/main/java/com/certacota/engine/core/service/TransactionService.java (interface analog):
- Plain Java interface, no Spring annotations, method signatures only

From engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java (current state):
- @TestConfiguration(proxyBeanMethods = false)
- @Bean @ServiceConnection PostgreSQLContainer<?> postgresContainer()
- Must ADD static GenericContainer<?> redis field + @DynamicPropertySource
</interfaces>
</context>

<phase_goal>
**As a** platform integrator, **I want to** start a streaming drain against a participant account and stop it to settle the exact elapsed amount, **so that** token flows are rate-billed correctly across pods without a database write per active tick.
</phase_goal>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flyway DDL migrations (V7/V8/V9) and dependency additions</name>
  <read_first>
    engine-service/src/main/resources/db/migration/V4__create_discrete_transactions.sql
    engine-service/src/main/resources/db/migration/V3__create_balance_audit_log.sql
    engine-service/build.gradle
    engine-spring/build.gradle
    .planning/phases/03-streaming-transactions/03-RESEARCH.md (§DDL section, §Standard Stack)
  </read_first>
  <files>
    engine-service/src/main/resources/db/migration/V7__create_streaming_transactions.sql,
    engine-service/src/main/resources/db/migration/V8__create_shedlock.sql,
    engine-service/src/main/resources/db/migration/V9__create_audit_archive.sql,
    engine-service/build.gradle,
    engine-spring/build.gradle
  </files>
  <action>
    V7__create_streaming_transactions.sql: Create table streaming_transactions with columns: id (BIGSERIAL PRIMARY KEY), stream_id (VARCHAR(255) NOT NULL), account_id (VARCHAR(255) NOT NULL), status (VARCHAR(20) NOT NULL DEFAULT 'ACTIVE'), rate_per_second (NUMERIC(38,18) NOT NULL), minimum_amount (NUMERIC(38,18) nullable), increment (NUMERIC(38,18) nullable), started_at (TIMESTAMPTZ NOT NULL), stopped_at (TIMESTAMPTZ nullable), settled_amount (NUMERIC(38,18) nullable), reason (VARCHAR(255) nullable), idempotency_key (VARCHAR(255) nullable). Constraints: uq_stream_id UNIQUE(stream_id), fk_str_account FK(account_id) REFERENCES accounts(id), chk_str_status CHECK(status IN ('ACTIVE','SETTLED','ERROR')), chk_str_rate CHECK(rate_per_second > 0). Indexes: idx_str_account_id ON streaming_transactions(account_id), idx_str_status ON streaming_transactions(status), uq_str_idempotency UNIQUE INDEX ON streaming_transactions(idempotency_key) WHERE idempotency_key IS NOT NULL. Use CONSTRAINT naming prefix convention matching V4 (pk_, uq_, fk_, chk_, idx_).

    V8__create_shedlock.sql: Create shedlock table with exactly: name VARCHAR(64) NOT NULL, lock_until TIMESTAMP NOT NULL, locked_at TIMESTAMP NOT NULL, locked_by VARCHAR(255) NOT NULL, PRIMARY KEY (name). No other columns — ShedLock requires this exact schema.

    V9__create_audit_archive.sql: CREATE SCHEMA IF NOT EXISTS audit_archive. Then CREATE TABLE audit_archive.balance_audit_log with columns mirroring public.balance_audit_log exactly: id BIGINT NOT NULL, account_id VARCHAR(255) NOT NULL, operation VARCHAR(50) NOT NULL, amount NUMERIC(38,18) NOT NULL, balance_before NUMERIC(38,18) NOT NULL, balance_after NUMERIC(38,18) NOT NULL, idempotency_key VARCHAR(255) nullable, transaction_id BIGINT nullable, recorded_at TIMESTAMPTZ NOT NULL. No foreign keys from audit_archive back to public schema. Add indexes: idx_arch_audit_account_id ON audit_archive.balance_audit_log(account_id), idx_arch_audit_recorded_at ON audit_archive.balance_audit_log(recorded_at).

    engine-spring/build.gradle: Add to dependencies block — implementation 'org.springframework.boot:spring-boot-starter-data-redis' (no version — BOM-managed), implementation 'org.redisson:redisson-spring-boot-starter:3.50.0', implementation 'net.javacrumbs.shedlock:shedlock-spring:6.6.0', implementation 'net.javacrumbs.shedlock:shedlock-provider-jdbc-template:6.6.0'. Read engine-spring/build.gradle first to see existing dependencies before adding.

    engine-service/build.gradle: Add same four dependencies to the dependencies block. The spring-boot-starter-data-redis version is managed by the Spring Boot plugin already present. Read engine-service/build.gradle first.
  </action>
  <behavior>
    - Flyway applies V7, V8, V9 in sequence without error when test suite starts
    - streaming_transactions has UNIQUE(stream_id) — duplicate streamId INSERT raises constraint violation
    - streaming_transactions rate_per_second > 0 CHECK fires on INSERT with rate = 0
    - shedlock table columns match exactly: name, lock_until, locked_at, locked_by
    - audit_archive schema contains balance_audit_log with same column types as public schema
    - engine-spring and engine-service compile with redis/redisson/shedlock on classpath
  </behavior>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota ; .\gradlew :engine-service:test --tests '*.CucumberTestRunner' -i 2>&1 | Select-String -Pattern 'BUILD|Flyway|migration|FAILED|ERROR' | Select-Object -First 30"</automated>
  </verify>
  <acceptance_criteria>
    - V7__create_streaming_transactions.sql contains CONSTRAINT chk_str_rate CHECK (rate_per_second > 0)
    - V7__create_streaming_transactions.sql contains CONSTRAINT uq_stream_id UNIQUE (stream_id)
    - V7__create_streaming_transactions.sql contains CONSTRAINT fk_str_account FOREIGN KEY (account_id) REFERENCES accounts(id)
    - V8__create_shedlock.sql contains exactly: name, lock_until, locked_at, locked_by columns and PRIMARY KEY (name)
    - V9__create_audit_archive.sql contains CREATE SCHEMA IF NOT EXISTS audit_archive
    - V9__create_audit_archive.sql contains audit_archive.balance_audit_log with recorded_at TIMESTAMPTZ
    - engine-spring/build.gradle contains redisson-spring-boot-starter:3.50.0
    - engine-service/build.gradle contains shedlock-spring:6.6.0
    - ./gradlew :engine-service:test --tests '*.CucumberTestRunner' logs show "Successfully applied 3 migrations" (V7, V8, V9) or total migration count increases by 3
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: engine-core contracts — StreamingTransaction entity, StreamState record, repository, service interfaces, DTOs, exceptions</name>
  <read_first>
    engine-core/src/main/java/com/certacota/engine/core/domain/DiscreteTransaction.java
    engine-core/src/main/java/com/certacota/engine/core/dto/CreditRequest.java
    engine-core/src/main/java/com/certacota/engine/core/dto/PostTransactionResponse.java
    engine-core/src/main/java/com/certacota/engine/core/exception/AccountNotFoundException.java
    engine-core/src/main/java/com/certacota/engine/core/repository/DiscreteTransactionRepository.java
    engine-core/src/main/java/com/certacota/engine/core/service/TransactionService.java
    .planning/phases/03-streaming-transactions/03-PATTERNS.md (§engine-core sections)
    .planning/phases/03-streaming-transactions/03-CONTEXT.md (D-07 through D-12)
  </read_first>
  <files>
    engine-core/src/main/java/com/certacota/engine/core/domain/StreamingTransaction.java,
    engine-core/src/main/java/com/certacota/engine/core/domain/StreamState.java,
    engine-core/src/main/java/com/certacota/engine/core/repository/StreamingTransactionRepository.java,
    engine-core/src/main/java/com/certacota/engine/core/service/StreamingService.java,
    engine-core/src/main/java/com/certacota/engine/core/service/StreamRegistry.java,
    engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamRequest.java,
    engine-core/src/main/java/com/certacota/engine/core/dto/StartStreamResponse.java,
    engine-core/src/main/java/com/certacota/engine/core/dto/StopStreamRequest.java,
    engine-core/src/main/java/com/certacota/engine/core/dto/StopStreamResponse.java,
    engine-core/src/main/java/com/certacota/engine/core/dto/EstimatedBalanceResponse.java,
    engine-core/src/main/java/com/certacota/engine/core/exception/StreamNotFoundException.java,
    engine-core/src/main/java/com/certacota/engine/core/exception/RedisUnavailableException.java,
    engine-core/src/main/java/com/certacota/engine/core/exception/StreamAlreadyActiveException.java
  </files>
  <action>
    StreamingTransaction.java: JPA entity annotated @Entity @Table(name = "streaming_transactions") @Getter @Builder @NoArgsConstructor @AllArgsConstructor. Package: com.certacota.engine.core.domain. Fields: Long id (@Id @GeneratedValue IDENTITY), String streamId (@Column(name="stream_id", nullable=false, updatable=false)), String accountId (@Column(name="account_id", nullable=false, updatable=false)), String status (@Column(name="status", nullable=false) — mutable, not updatable=false), BigDecimal ratePerSecond (@Column(name="rate_per_second", nullable=false, updatable=false, precision=38, scale=18)), BigDecimal minimumAmount (@Column(name="minimum_amount", precision=38, scale=18) nullable, updatable=false), BigDecimal increment (@Column(name="increment", precision=38, scale=18) nullable, updatable=false), OffsetDateTime startedAt (@Column(name="started_at", nullable=false, updatable=false)), OffsetDateTime stoppedAt (@Column(name="stopped_at") mutable nullable), BigDecimal settledAmount (@Column(name="settled_amount", precision=38, scale=18) mutable nullable), String reason (@Column(name="reason") mutable nullable), String idempotencyKey (@Column(name="idempotency_key", updatable=false) nullable). No metadata field. No @Enumerated — status is stored as String. Status constants as public static final String: ACTIVE = "ACTIVE", SETTLED = "SETTLED", ERROR = "ERROR".

    StreamState.java: Java record in package com.certacota.engine.core.domain. Fields: String streamId, String accountId, BigDecimal ratePerSecond, long startedAtEpochMillis, long startedAtNano, boolean startedAtNanoFromCurrentJvm, BigDecimal minimumAmount (nullable), BigDecimal increment (nullable). Include static factory: public static StreamState fromRedis(String streamId, Map fields) — parses each field from the String-valued map (ratePerSecond via new BigDecimal(value), minimumAmount/increment return null if value is empty string, startedAtNanoFromCurrentJvm is always false when loading from Redis since Redis entries may be from any pod). Include static factory: public static StreamState fromDbRow(StreamingTransaction txn, long jvmStartNano) — for startup reconciliation; startedAtNano is computed as (currentNanoTime - (currentMillis - txn.startedAt.toInstant().toEpochMilli()) * 1_000_000), startedAtNanoFromCurrentJvm = false (post-restart, use wall-clock).

    StreamingTransactionRepository.java: Interface extending JpaRepository of StreamingTransaction, Long. Package com.certacota.engine.core.repository. Methods: Optional findByStreamId(String streamId), List findByAccountIdAndStatus(String accountId, String status), List findByStatus(String status), boolean existsByAccountIdAndStatus(String accountId, String status). All return types must match the above exactly.

    StreamRegistry.java: Plain Java interface in com.certacota.engine.core.service. No Spring, no Redis imports. Methods: void register(StreamState state), Optional get(String streamId), void remove(String streamId, String accountId), List getActiveStreams(String accountId), boolean hasActiveStreams(String accountId).

    StreamingService.java: Plain Java interface in com.certacota.engine.core.service. Methods: StartStreamResponse startStream(StartStreamRequest request), StopStreamResponse stopStream(String streamId, StopStreamRequest request), EstimatedBalanceResponse estimateBalance(String accountId), void autoTerminate(String streamId).

    StartStreamRequest.java: Java record in com.certacota.engine.core.dto. Fields per D-07: @NotBlank String streamId, @NotBlank String accountId, @NotNull @Positive BigDecimal ratePerSecond, @NotBlank String idempotencyKey, @Positive BigDecimal minimumAmount (nullable — no @NotNull), @Positive BigDecimal increment (nullable — no @NotNull). Import jakarta.validation.constraints.*.

    StartStreamResponse.java: Java record in com.certacota.engine.core.dto. Fields per D-08: String streamId, String accountId, BigDecimal ratePerSecond, OffsetDateTime startedAt, BigDecimal minimumAmount (nullable), BigDecimal increment (nullable). Include static factory: public static StartStreamResponse from(StreamingTransaction txn).

    StopStreamRequest.java: Java record. Fields per D-09: boolean ignoreMinimum (primitive — default false when deserialized from JSON missing field), String reason (nullable — default "stop endpoint call" applied in service layer, not here).

    StopStreamResponse.java: Java record. Fields per D-10: BigDecimal settledAmount, OffsetDateTime stoppedAt, String reason. No finalBalance field.

    EstimatedBalanceResponse.java: Java record. Fields per D-12: BigDecimal estimatedBalance, BigDecimal committedBalance, OffsetDateTime estimatedAt, Long estimatedDrainAt (nullable — Long not long, null when no active streams or drain rate is zero).

    StreamNotFoundException.java, RedisUnavailableException.java, StreamAlreadyActiveException.java: Each extends RuntimeException with single-arg String constructor calling super("Stream not found: " + streamId), super(message), super("Stream already active: " + streamId) respectively. Package com.certacota.engine.core.exception.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota ; .\gradlew :engine-core:build 2>&1 | Select-String -Pattern 'BUILD|error|FAILED' | Select-Object -First 20"</automated>
  </verify>
  <acceptance_criteria>
    - ./gradlew :engine-core:build exits 0 with BUILD SUCCESSFUL
    - StreamingTransaction.java contains @Table(name = "streaming_transactions") and constants ACTIVE = "ACTIVE", SETTLED = "SETTLED", ERROR = "ERROR"
    - StreamState.java is a record with startedAtNanoFromCurrentJvm field and static fromRedis() and fromDbRow() factory methods
    - StreamingTransactionRepository.java contains existsByAccountIdAndStatus method
    - StreamRegistry.java is a pure Java interface with no Spring or Redis imports
    - StreamingService.java interface contains autoTerminate(String streamId) method
    - StopStreamResponse.java has no finalBalance field
    - EstimatedBalanceResponse.java uses Long (not long) for estimatedDrainAt
    - All three exception classes exist in com.certacota.engine.core.exception package
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Test scaffolding — Redis Testcontainers, all 6 feature files, StreamingSteps with pending stubs</name>
  <read_first>
    engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java
    engine-service/src/test/java/com/certacota/engine/service/steps/TransactionSteps.java
    engine-service/src/test/resources/features/discrete-credit.feature
    engine-service/src/test/resources/features/discrete-debit.feature
    .planning/phases/03-streaming-transactions/03-VALIDATION.md (§Wave 0 Requirements)
    .planning/phases/03-streaming-transactions/03-PATTERNS.md (§feature files, §StreamingSteps)
  </read_first>
  <files>
    engine-service/src/test/java/com/certacota/engine/service/TestcontainersConfiguration.java,
    engine-service/src/test/resources/features/streaming-start.feature,
    engine-service/src/test/resources/features/streaming-stop.feature,
    engine-service/src/test/resources/features/streaming-estimation.feature,
    engine-service/src/test/resources/features/streaming-minimum-amount.feature,
    engine-service/src/test/resources/features/streaming-increment.feature,
    engine-service/src/test/resources/features/streaming-auto-termination.feature,
    engine-service/src/test/java/com/certacota/engine/service/steps/StreamingSteps.java
  </files>
  <action>
    TestcontainersConfiguration.java: Add a static GenericContainer field alongside the existing @Bean PostgreSQLContainer. Declare: static GenericContainer redis = new GenericContainer(DockerImageName.parse("redis:7-alpine")).withExposedPorts(6379). Add a companion @TestConfiguration class or inner static class annotated @DynamicPropertySource: static void redisProperties(DynamicPropertyRegistry registry) { registry.add("spring.data.redis.host", redis::getHost); registry.add("spring.data.redis.port", () -> String.valueOf(redis.getMappedPort(6379))); }. The Redis container must be started before @DynamicPropertySource fires — declare it as a static field (initialized at class load) so it is available. Import org.testcontainers.containers.GenericContainer and org.springframework.test.context.DynamicPropertyRegistry and org.springframework.test.context.DynamicPropertySource.

    streaming-start.feature: Feature: Stream start. Include scenarios covering: (a) start stream with valid rate returns 201 with streamId echoed back; (b) start stream on non-existent account returns 404; (c) start stream on CLOSED account returns 409; (d) start stream with idempotency key returns same response on duplicate request. Use account IDs like "stream-start-001", "stream-start-002" etc to avoid cross-scenario contamination. Each scenario begins with "Given no account with id ... exists" step.

    streaming-stop.feature: Feature: Stream stop. Scenarios: (a) start then stop returns 200 with settledAmount > 0 and reason "stop endpoint call"; (b) stop non-existent streamId returns 404; (c) stop on wrong accountId returns 404 (account-stream ownership check); (d) settling stream charges actual elapsed, Postgres row shows status SETTLED.

    streaming-estimation.feature: Feature: Forward balance estimation. Scenarios: (a) GET /accounts/{id}/estimated-balance with one active stream returns estimatedBalance less than committedBalance, estimatedAt populated, estimatedDrainAt non-null; (b) same endpoint with no active streams returns estimatedBalance == committedBalance, estimatedDrainAt null; (c) response includes committedBalance field equal to Postgres committed balance.

    streaming-minimum-amount.feature: Feature: Minimum amount enforcement. Scenarios: (a) stop a stream before minimumAmount is reached settles minimumAmount; (b) stop with ignoreMinimum=true settles actual elapsed even when less than minimumAmount; (c) start stream without minimumAmount parameter — minimumAmount absent from response.

    streaming-increment.feature: Feature: Increment billing. Scenarios: (a) stop a stream with increment set — settled amount equals floor(elapsed * rate / increment) * increment, not exact elapsed; (b) start stream without increment — settles exact elapsed; (c) verifying increment remainder stays on account after settlement.

    streaming-auto-termination.feature: Feature: Auto-termination. Scenarios: (a) account balance fully drained by active stream — stream auto-terminates with reason "balance_exhaustion"; (b) audit log entry for auto-terminated stream has reason "balance_exhaustion"; (c) auto-terminated stream settles actual elapsed (ignoreMinimum=true behavior).

    StreamingSteps.java: Package com.certacota.engine.service.steps. Annotate @Slf4j. Use same class-level injection pattern as TransactionSteps: @Autowired private StreamingTransactionRepository streamingTransactionRepository, @Autowired private SharedContext sharedContext, @LocalServerPort private int port, RestTemplate restTemplate (constructed in constructor with DefaultResponseErrorHandler override that always returns false for hasError). Declare all @Given/@When/@Then step methods matching every step in the 6 feature files. Each method body: throw new io.cucumber.java.PendingException(). This makes tests fail at the correct step with "Step is pending" rather than with glue code errors. Import all required types. The step methods will be fully implemented in Plan 04.
  </action>
  <verify>
    <automated>powershell -Command "cd C:\Mergebine\certacota ; .\gradlew :engine-service:test --tests '*.CucumberTestRunner' -i 2>&1 | Select-String -Pattern 'BUILD|Pending|PENDING|streaming|feature' | Select-Object -First 30"</automated>
  </verify>
  <acceptance_criteria>
    - TestcontainersConfiguration.java contains static GenericContainer redis field and @DynamicPropertySource method registering spring.data.redis.host and spring.data.redis.port
    - All 6 feature files exist under engine-service/src/test/resources/features/ with names streaming-start.feature, streaming-stop.feature, streaming-estimation.feature, streaming-minimum-amount.feature, streaming-increment.feature, streaming-auto-termination.feature
    - Each feature file has at least 3 Scenario blocks covering the target requirement
    - StreamingSteps.java compiles and contains @Given/@When/@Then methods matching all step signatures across the 6 feature files
    - ./gradlew :engine-service:test --tests '*.CucumberTestRunner' output shows streaming scenarios as PENDING (not as undefined steps or compilation errors)
    - Existing Phase 1 and Phase 2 Cucumber scenarios remain GREEN (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway migrations → Postgres | DDL statements executed with database admin privileges at startup |
| build.gradle → Maven Central | Dependency declarations pull transitive code into runtime classpath |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | V7 DDL — rate_per_second column | mitigate | chk_str_rate CHECK (rate_per_second > 0) prevents zero/negative rate insert at DB level; @Positive on StartStreamRequest DTO provides API-layer guard in Plan 02 |
| T-3-02 | Tampering | V7 DDL — stream_id uniqueness | mitigate | CONSTRAINT uq_stream_id UNIQUE (stream_id) prevents double-start of same streamId at DB level; idempotency key check after lock in Plan 02 provides application-layer guard |
| T-3-03 | Tampering | StreamingTransaction.status field | mitigate | status is a non-updatable column in JPA except via explicit setter — plan marks it mutable only for settlement state transitions (ACTIVE → SETTLED/ERROR); direct SQL manipulation to revert SETTLED → ACTIVE is blocked only by application trust boundary |
| T-3-04 | Information Disclosure | audit_archive schema | accept | archive schema is on same Postgres instance; no FK back to public schema is an intentional design to allow archival rows to outlive main records; no sensitive data beyond what balance_audit_log already contains |
| T-3-05 | Denial of Service | Testcontainers Redis container | accept | test-only component; GenericContainer.withExposedPorts(6379) binds to a random host port per RESEARCH.md Pattern 7 — no fixed port collision risk |
</threat_model>

<verification>
After all three tasks complete:
- ./gradlew :engine-core:build exits 0
- ./gradlew :engine-service:test --tests '*.CucumberTestRunner' shows V7/V8/V9 migrations applied and streaming scenarios in PENDING state
- No regressions in Phase 1/2 Cucumber scenarios
- grep -r "rate_per_second > 0" engine-service/src/main/resources/db/migration/V7__create_streaming_transactions.sql returns a match
- grep -r "audit_archive" engine-service/src/main/resources/db/migration/V9__create_audit_archive.sql returns a match
</verification>

<success_criteria>
- Three Flyway migrations (V7/V8/V9) apply cleanly in the Testcontainers Postgres instance with no errors
- engine-core module compiles with 13 new files and zero compilation errors
- StreamRegistry and StreamingService interfaces contain no Spring or Redis imports (pure Java)
- All 6 streaming feature files exist and produce PENDING (not undefined) steps in the test output
- StreamingSteps.java compiles and throws PendingException from each step method
- TestcontainersConfiguration.java provides Redis connection properties to Spring test context via @DynamicPropertySource
- Phase 1 and Phase 2 tests remain GREEN (zero regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-streaming-transactions/03-01-SUMMARY.md`
</output>
